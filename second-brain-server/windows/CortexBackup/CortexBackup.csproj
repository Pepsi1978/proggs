<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net10.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <UseWPF>true</UseWPF>
    <AssemblyName>CortexBackup</AssemblyName>
    <Product>Cortex Backup</Product>
    <Version>1.0.0</Version>
    <ApplicationIcon>cortex.ico</ApplicationIcon>
    <!-- WPF: KEIN PublishTrimmed/AOT (bugs/desktop/dotnet-csharp.md, Abschnitt 1.5). Single-File und
         Self-Contained werden im publish.ps1 per CLI gesetzt (RID win-x64, self-contained true). -->
  </PropertyGroup>

</Project>
