// HarnessForgeLayers.swift
// Platzhalter-Modul fuer die sechs Harness-Schichten (L1–L5 + Meta).
//
// Wird ab Step 4 mit Inhalten gefuellt. Hier lediglich ein oeffentlicher
// Namespace, damit das Target uebersetzt und testbar bleibt.

import Foundation

/// Hauptnamespace der Harness-Schichten-Bibliothek.
public enum HarnessForgeLayers {
    /// Versionsnummer, gekoppelt an den Core.
    public static let version = "0.1.0"
}
