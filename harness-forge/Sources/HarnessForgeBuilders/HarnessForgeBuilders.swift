// HarnessForgeBuilders.swift
// Platzhalter-Modul fuer die fuenf Harness-Builder (Pure Prompt, Python CLI,
// Tauri Desktop, Android Kotlin, Claude Subagent) plus den TaskAnalyzer.
//
// Wird ab Step 5 mit Inhalten gefuellt. Hier lediglich ein oeffentlicher
// Namespace, damit das Target uebersetzt und testbar bleibt.

import Foundation

/// Hauptnamespace der Harness-Builder-Bibliothek.
public enum HarnessForgeBuilders {
    /// Versionsnummer, gekoppelt an den Core.
    public static let version = "0.1.0"
}
