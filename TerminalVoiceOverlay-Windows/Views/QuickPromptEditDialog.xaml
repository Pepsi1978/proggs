<Window x:Class="TerminalVoiceOverlay.Views.QuickPromptEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompt bearbeiten"
        Width="640" Height="480"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
    <Border CornerRadius="16"
            Background="#FF1E1E1E"
            BorderBrush="#FF3A3A3A"
            BorderThickness="1"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock x:Name="HeaderText" Grid.Row="0"
                       Text="Prompt bearbeiten"
                       Foreground="#FFEEEEEE"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Foreground="#FF9A9A9A" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,8"
                       Text="Dieser Text wird beim Linksklick auf die Zahl in die Befehlszeile eingefügt. Leer lassen, um den Platz frei zu machen."/>

            <TextBox x:Name="PromptBox" Grid.Row="2"
                     Background="#FF2D2D2D" Foreground="White"
                     BorderBrush="#FF454545" BorderThickness="1"
                     Padding="8,6" FontSize="13"
                     AcceptsReturn="True" AcceptsTab="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button x:Name="BtnCancel"
                        Content="Abbrechen"
                        Background="#FF2D2D2D" Foreground="#FFCCCCCC"
                        BorderBrush="#FF454545" BorderThickness="1"
                        Padding="12,6" Margin="0,0,8,0" Cursor="Hand"
                        IsCancel="True"/>
                <Button x:Name="BtnSave"
                        Content="Speichern"
                        Background="#FFB8860B" Foreground="White"
                        BorderThickness="0" Padding="16,6" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
