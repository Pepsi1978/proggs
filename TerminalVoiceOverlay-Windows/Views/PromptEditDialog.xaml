<Window x:Class="TerminalVoiceOverlay.Views.PromptEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prompt"
        Width="560" Height="520"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ResizeMode="NoResize">
    <Border CornerRadius="16"
            Background="#FF1E1E1E"
            BorderBrush="#FF3A3A3A"
            BorderThickness="1"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       x:Name="TitleText"
                       Text="Prompt bearbeiten"
                       Foreground="#FFEEEEEE"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,12"/>

            <TextBlock Grid.Row="1" Text="Kurzbezeichnung" Foreground="#FFCCCCCC" FontSize="12" Margin="0,0,0,4"/>
            <TextBox Grid.Row="2"
                     x:Name="ShortLabelBox"
                     Background="#FF2D2D2D"
                     Foreground="White"
                     BorderBrush="#FF454545"
                     BorderThickness="1"
                     Padding="8,6"
                     FontSize="13"
                     Margin="0,0,0,10"/>

            <TextBlock Grid.Row="3" Text="Prompt-Text" Foreground="#FFCCCCCC" FontSize="12" Margin="0,0,0,4"/>
            <TextBox Grid.Row="4"
                     x:Name="OriginalTextBox"
                     Background="#FF2D2D2D"
                     Foreground="White"
                     BorderBrush="#FF454545"
                     BorderThickness="1"
                     Padding="8,6"
                     FontSize="13"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,10"/>

            <CheckBox Grid.Row="5"
                      x:Name="AlwaysOnCheckbox"
                      Foreground="#FFEEEEEE"
                      Content="Immer mitschicken (Always-On Prefix)"
                      FontSize="12"
                      Margin="0,0,0,12"/>

            <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel"
                        Content="Abbrechen"
                        Background="#FF2D2D2D"
                        Foreground="#FFCCCCCC"
                        BorderBrush="#FF454545"
                        BorderThickness="1"
                        Padding="12,6"
                        Margin="0,0,8,0"
                        Cursor="Hand"/>
                <Button x:Name="BtnOk"
                        Content="Speichern"
                        Background="#FFB8860B"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="16,6"
                        Cursor="Hand"
                        IsDefault="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
