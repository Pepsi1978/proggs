<resources>
    <style name="Theme.Wecker" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:colorAccent">#8B6914</item>
    </style>
    <!-- Nur das Startfenster des Weckbildschirms: Bis Compose zeichnet, blendet sonst die helle
         Material-Vorlage auf, gerade wenn der Alarm den Bildschirm nachts einschaltet. Die Oberfläche
         selbst folgt weiterhin der gewählten Hell-/Dunkel-Einstellung. -->
    <style name="Theme.Wecker.Alarm" parent="Theme.Wecker">
        <item name="android:windowBackground">#FF121212</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
