{
	"description": "finale hooks: skill-symlink-verification at session start, strings.xml lint after write/edit. Cross-Platform Dual-Registration: jeder Hook hat .sh + .ps1 registriert. Die .ps1-Hooks pruefen am Anfang ob 'bash' im PATH ist und exiten still wenn ja — so laeuft auf jeder Plattform genau eine Variante. HINWEIS: Die blockierenden PreToolUse-Guards (destructive-bash-guard, audit-only-write-guard) wurden am 2026-07-08 auf Wunsch entfernt (Session-Backup wird manuell ausgeloest; der fail-closed python3-Check konnte die ganze Session blockieren). Die .sh/.ps1-Dateien liegen noch im Ordner, sind aber nicht mehr registriert.",
	"hooks": {
		"SessionStart": [
			{
				"matcher": "startup|clear|compact|resume",
				"hooks": [
					{
						"type": "command",
						"command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\"",
						"async": false
					},
					{
						"type": "command",
						"command": "pwsh -NoProfile -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.ps1\"",
						"async": false
					}
				]
			}
		],
		"PostToolUse": [
			{
				"matcher": "Edit|Write|MultiEdit",
				"hooks": [
					{
						"type": "command",
						"command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/posttooluse-strings-xml.sh\"",
						"async": false
					},
					{
						"type": "command",
						"command": "pwsh -NoProfile -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/hooks/posttooluse-strings-xml.ps1\"",
						"async": false
					}
				]
			}
		]
	}
}
