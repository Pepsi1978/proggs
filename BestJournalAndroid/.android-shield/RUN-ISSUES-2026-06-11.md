# finale Plugin — RUN-ISSUES 2026-06-11

> Auto-generiert vom Orchestrator (FIN-047). Konsolidiert plugin_bugs_observed
> aller Subagents des Vollscan-Laufs cbf88768 (2026-06-10/11).

## Orchestrator-Beobachtungen (uebergreifend)

| # | Symptom | Evidence | Suggestion |
|---|---------|----------|------------|
| O1 | Bash-Heredoc verstuemmelt Backslashes/Apostrophe in Python-Code | 6+ Worker + Orchestrator selbst (re-Lookbehind wurde zu PatternError) | Worker-Template haerten: Python IMMER als .py via Write-Tool, NIE Heredoc — jetzt in allen Prompts, sollte in translation-worker.md Abschnitt ENV |
| O2 | pretooluse-bash-Guard blockt python -c auch bei reinem Read | 8+ Worker meldeten verlorene Runde | Guard ok (Sicherheit), aber Template soll .py-via-Write fuer INSPEKTION explizit vorgeben |
| O3 | Worker-Template kennt nur additiven Insert (skip-if-present), Phase-3-Delta ist aber REPLACE | en/kn-Worker | translation-worker.md um REPLACE-Modus + Lambda-Replacement-Warnung (re.sub Backslash-Falle) ergaenzen |
| O4 | tr-Apostrophe (Suffixe nach Eigennamen) nicht in FIN-044-Validator-Pflicht | tr-Worker fand 6 Build-Blocker in eigenen frischen Texten | FIN-044 um tr erweitern; zentrale Apostroph-Pruefung nach JEDER Welle (Orchestrator hat en/nl/uk mit 22 Fixes nachziehen muessen) |
| O5 | feature-scan.sh: 3 Bash-Fehler (sed-Sonderzeichen, arithmetic, HEALTH_MAP unbound) + Locale-Parser ordnet pt-BR/pt-PT/zh-CN als default ein | Lauf 2026-06-10 | Skript fixen (separater Harness-Task) |
| O6 | job-plan count-Feld zaehlt strings+plurals zusammen, verwirrt Worker | en/kn/te | Felder getrennt ausweisen |
| O7 | legal-keys-Extraktion erzeugte 2 Pseudo-Keys (onboarding_feature_secure__privacy, Composite-Notationen) | L1/L4/L5 | Key-Validierung gegen DE-strings.xml vor Worker-Spawn |
| O8 | formatArgMismatch-Matrix verfehlt Stale-Drift ohne Arg-Unterschied | L8 (pt-rBR rerank-Stub 459 vs 1864 Zeichen) | lengthDriftSuspect-Spalte (loc/de-Laengenverhaeltnis < 0.4) ergaenzen |
| O9 | identicalToDe ohne doNotTranslate-Allowlist (json_key_*, Marken, Monatsnamen) | L7: nl 39 -> real 3 | Allowlist in Matrix-Skript |
| O10 | Session-Limit crashte 5 Welle-4-Worker (0 Output) | 2026-06-10 ~23h | FIN-052-Resume griff sauber: 0 Datenverlust, identischer Respawn am Folgetag |
| O11 | FIN-032-Formulierung zirkulaer fuer Translation-Worker (Worker IST der Skill-Ausfuehrende) | gu-Worker | Template-Praezisierung: Skill-References lesen ersetzt Skill-Call im Worker-Kontext |

## Worker-gemeldete Einzelbeobachtungen

| Worker | Beobachtung |
|--------|-------------|
| w7-hidden-assets | FIN-OBS-W7-1: Inline Bash heredoc (Bash tool) mangled the Python escape sequence rel.replace('\\','/') into a single backslash, causing a SyntaxError - the known-error hook matched it at 67%. Workaround: write Python to a file in the write-zone and run it. Recommendation: worker prompt should instru |
| w7-hidden-assets | FIN-OBS-W7-2: Grep tool requires an ABSOLUTE path on Windows; the relative app/src/main/java path failed because cwd is C:/Users/barwa/proggs (parent of APP-ROOT). The worker prompt gives APP-ROOT but the harness cwd is one level up - prompts should state that Grep/Glob need the full C:/.../BestJour |
| w7-hidden-assets | FIN-OBS-W7-3: Worker baseline assumption ('3 full legal versions') was outdated vs. code reality (5). Audit prompts that hard-code a baseline count should phrase it as 'verify the count' (which this one did) rather than 'confirm 3' - good prompt design; keep it. |
| w8-literals | FIN-037: Grep auf named-Parameter (text=/title=/label=) ist verrauscht — Compose-Animations-`label = "..."` (rememberInfiniteTransition/animate*) dominieren die Treffer und sind NIE nutzersichtbar. Empfehlung fuer kuenftige Worker-Prompts: Animations-label per Negativ-Filter (`rememberInfiniteTransi |
| w8-literals | FIN-037: Der praeziseste Single-Pass fuer i18n-Leaks in DE-Apps ist der Umlaut-/ss-Sweep ("[A-Za-z][^"]*[äöüÄÖÜß][^"]*") — er fand ALLE relevanten Faelle in EINEM Grep, waehrend die 9 Standardmuster fast nur Symbole/Interpolation lieferten. Sollte als ERSTER Pattern-Durchlauf laufen. |
| w8-literals | FIN-037: 'userVisible' laesst sich aus einem Literal allein NICHT bestimmen — AdviceCategory.displayName SIEHT nutzersichtbar aus, ist aber nur ein Lookup-Key (echte Anzeige via displayNameResId). Ein reiner Grep-Worker wuerde das falsch als Leak melden; Kontext-Read der Verwendungsstelle ist Pflich |
| w1-manifest-build | Spec-Luecke: Die Aufgabenstellung listet nur Gradle-deklarierte SDKs als Drittanbieter-Quellen, aber der rechtlich relevanteste Datenfluss (Microsoft Bing Edge-TTS) ist ein direkter WSS-Call OHNE Gradle-Dependency. Der Roentgen-Skill sollte den https/wss-Grep (Layer-5) EXPLIZIT als gleichwertige Que |
| w1-manifest-build | Redundanz/Unschaerfe: sendsDataToThirdParty bei Google-Drive/Gmail ist mehrdeutig, die Daten gehen in den EIGENEN Drive/das eigene Gmail des Nutzers (Auftragsverarbeitung Google), nicht an einen fremden Empfaenger. Das Schema sollte zwischen Empfaenger=Drittanbieter-Service des Anbieters und Speiche |
| w5-paywall | FIN-037 self-observation: The scope prompt lists 'BillingManager.kt (~part of Bucket K3, Datei ist mittelgross - voll lesbar)' but the file is 75 KB (~19k tokens) - borderline against the FIN-048 token budget. I read it via targeted Grep + Read ranges instead of a full read; suggest the bucket sizin |
| w5-paywall | Worker-output schema lacks a dedicated field for 'pricing constants that COULD be shown but were not verified on every screen' - I had to fold the RETENTION_*_PRICE divergence risk into criticalObservations. A 'crossScreenVerificationNeeded' array would let the orchestrator route a precise follow-up |
| w5-paywall | ChurnFlowDialog.kt (50 KB) and PaywallScreen.kt (49 KB) sit just under the 100 KB Grep-only threshold but are still large; reading them in 200-230 line ranges worked, but the threshold guidance ('>100KB -> only ranges') could be lowered to ~40KB for Compose screen files, which are token-dense (deep  |
| w6-screens-arch | Worker cwd reset to ~/proggs between Bash calls (NOT the app root given in the briefing): the first tool batch using relative 'app/src/main/java' failed with 'Path does not exist'. Recommendation: orchestrator should inject ABSOLUTE paths into the worker prompt examples, or worker must always prefix |
| w6-screens-arch | Inline heredoc (<< 'PYEOF') with a large Python script containing an apostrophe (the word user's) failed with bash 'unexpected EOF while looking for matching quote' - the surrounding Bash-tool quoting interacted badly with the heredoc. Robust fix: Write the Python to a .py file then run it, instead  |
| w6-screens-arch | OVERSIZE-rule (FIN-048) worked as intended: Grep with -C/head_limit + Python head-slices on SettingsScreen.kt (289KB) and DashboardScreen.kt (193KB) returned full dialog/nav maps without ever loading them into context - lossless, no crash, whole Layer-3+4 scan stayed well under limit. |
| w2-strings-a | Heredoc-Crash: Python-Code mit deutschen Strings (Apostrophe) ueber bash -c Heredoc an die Shell zu geben bricht beim ersten Apostroph (unexpected EOF). Worker sollten Output-Generatoren als .py-Datei (Write-Tool) schreiben und ausfuehren, statt UNTRUSTED_APP_DATA inline ins Heredoc zu legen. |
| w2-strings-a | bug-almanac-guard blockierte den ERSTEN Write auf gen_w2_output.py (Bereich python-windows), Write meldete aber faelschlich Erfolg waehrend die Datei NICHT angelegt wurde. Folge-Bash sah 'No such file'. Worker im finale-Plugin sollten den Python-Almanach-Kurzcheck VORAB lesen (oder Pipeline setzt ei |
| w2-strings-a | Read-Tool mit offset=1 limit=500 lieferte zuverlaessig ~58KB ohne 256KB-Block — bestaetigt dass der Strings-Scan in 500-Zeilen-Chunks crash-sicher ist (kein Voll-Read der 1494-Zeilen-Datei noetig). |
| w3-strings-b | Read-Tool blockiert in diesem Scope HART: strings.xml (1494 Z., ~44.6k Tokens) liess sich NICHT mit Read offset/limit lesen - selbst ein 250-Zeilen-Slice (94KB) ueberschritt den 25k-Token-Cap, weil die DSGVO-/AI-Prompt-Strings extrem dicht sind (40 Zeilen = 30KB). Workaround: per Python in Mikro-Chu |
| w3-strings-b | PostToolUseFailure-Hook matchte bei JEDEM Read-Token-Cap-Fehler den irrelevanten Bug-Case 'LFS Push-Rejection / .gitattributes' (50% Aehnlichkeit). Falsch-Positiv: der Fehler war ein LLM-Kontext-Token-Limit, kein Git/LFS-Problem. Der 50%-Schwellwert des Auto-Matchers verwechselt 'Datei zu gross fuer |
| w3-strings-b | Bash-Heredoc <<'PY' mit eingebettetem Python-Skript voller deutscher Apostrophe/einfacher Anfuehrungszeichen brach mit 'unexpected EOF while looking for matching quote' (Exit 2). Robuster Fix: Python-Skript zuerst mit dem Write-Tool in eine .py-Datei schreiben (kein Shell-Quoting) und dann mit 'pyth |
| w3-strings-b | Output-Schema-Hinweis: 'Harmlose UI-Labels nicht in findings - nur zaehlen' kollidiert mit 'Grenzfaelle IMMER aufnehmen (NIEDRIG)'. Bei einem DSGVO-/Consent-lastigen Block ist fast jeder String ein potenzieller Grenzfall - die Grenze zu 'harmlos' bleibt subjektiv. Klarere Schwelle (z.B. 'nur Strings |
| w4-strings-c | FIN-037: 'Unbegrenzt'-Werbung (retro_benefit_weekly 1035, paywall_feature_profiles 1125, churn_offer_feature_perspectives 1177, paywall_feature_followups 1420) steht in direktem Spannungsverhaeltnis zur KI-Kontingent-Aussage ai_limits_dialog_body (1199, 150/Tag/Profil). Empfehlung Phase-2: dediziert |
| w4-strings-c | Button-Wording-Inkonsistenz ist ein systematisches Muster: 'Kostenlos starten' (1083, OHNE Zahlungspflicht-Hinweis) vs. 'Zahlungspflichtig - 2 Monate zum halben Preis' (1144, MIT Hinweis). Roentgen-Output sollte CTAs separat als 'Button-Loesung 312j'-Kategorie clustern, damit das Audit alle Buttons  |
| w4-strings-c | Methodik: strings.xml allein zeigt nicht, WO ein String erscheint (ob 'Kostenlos starten' wirklich einen Bezahl-Trial startet). Vorschlag: Phase-1a-Output kuenftig mit leichtem Code-Grep nach R.string.<key>-Verwendungsstellen anreichern (separater Worker), um Risiko-Einstufungen praeziser zu machen. |
| r3-abo | Input recht-input-abo.json (mandatoryDisclosures.gaps) beschreibt den Para-356-Abs.5-Punkt als 'closed (D1-Fix 2026-05-31)', aber die Quell-Verifikation zeigt: der Verzichts-Dialog existiert NICHT (nur ein Lade-Toast unter dem irrefuehrenden Kommentar strings.xml:1100). Der Input uebernahm den optim |
| r3-abo | Input nennt fuer den Churn-Cancel-CTA durchgaengig alpha 0.6 (ChurnFlowDialog.kt:1087). In der aktuellen Datei gibt es ZWEI verschiedene Cancel-Stellen: der Step-0-Overview-Link mit alpha 0.8f (Z.504, 'Frank-Wahl Option [3]') und der entscheidende Step-3-Link mit alpha 0.6f (Z.1092). Ein partieller  |
| l1 | Hinweis (kein Plugin-Bug): legal-keys-Liste enthaelt einen Nicht-Schluessel-Eintrag 'onboarding_feature_secure__privacy' (doppelter Unterstrich), der in DE nicht existiert — vermutlich Notiz/Tippfehler in der Schluesselliste. Der reale Schluessel ist 'onboarding_feature_secure'. |
| l1 | Hinweis: 'ai_prompt_insight_intro / profile_insight_*' und 'urgency_high / legend_high_burden' in der legal-keys-Liste sind kombinierte Notiz-Eintraege mit ' / ' bzw. Wildcard '*' — als getrennte konkrete Keys interpretiert (insgesamt 95 konkrete Keys + 1 Wildcard profile_insight_*). |
| L2 | pretooluse-bash.sh blockierte korrekt "python -c" inline-Code mit App-Pfad-Referenz (Schreibsperre) — erwartet/by-design, kein Bug; Workaround: .py-Datei via Write. Hinweis: die Block-Meldung feuert auch bei reinen READ-Inline-Skripten (mein l2_jsonkey_compare war read-only) — leicht ueberbreiter Tr |
| L2 | cross-lingual-matrix-2026-06-10.json identicalToDe ist akkurat fuer alle 3 Sprachen (Stichprobe bestaetigt). formatArgMismatch/nakedPercent/apostrophe-Maschinencheck deckte sich mit manueller Pruefung (0 echte Treffer). |
| L2 | L2-Hinweis fuer Schema-Konsolidierung: meine l2_legal_pairs.py XML-Extraktion gab bei Keys, die nicht als reines <string> vorlagen, faelschlich None (ai_prompt_rerank_*, profile_insight_*). Direkt-Read korrigierte das. Kein Plugin-Bug, aber Worker-Methodik-Notiz. |
| L3-cross-lingual-legal | Kein Bug. Hinweis: pretooluse-bash.sh blockiert JEDE Scripting-Sprache mit Code-Arg das eine GESCHUETZTE Datei referenziert — auch reine READ-Operationen (open(...,'r')). Fuer reine Audit-Worker etwas streng, aber sicherheitstechnisch korrekt (Defense-in-Depth). .py-Dateien via Write umgehen es saub |
| L4 | {'severity': 'info', 'observation': 'pretooluse-bash.sh blockiert korrekt inline "python -c" das den App-res-Pfad referenziert (SCHREIB-SPERRE greift auch bei Read-only-Absicht). Workaround: .py-Datei via Write-Tool ins .android-shield/-Scope. Funktioniert, aber Read-only-Inspektion eines App-Pfads  |
| L4 | {'severity': 'info', 'observation': 'legal-keys-2026-06-10.json enthaelt 2 nicht-direkt-aufloesbare Eintraege: Wildcard "profile_insight_*" und doppelter Marker "ai_prompt_insight_intro / profile_insight_*" sowie "onboarding_feature_secure__privacy" (doppelter Unterstrich) - nicht als echte Keys in  |
| L5-cjk | Keine finale-Plugin-Bugs beobachtet. Inputs (legal-keys, cross-lingual-matrix) sauber und konsistent. Hinweis: der Legal-Key 'onboarding_feature_secure__privacy' (sowie Composite-Eintraege wie 'ai_prompt_insight_intro / profile_insight_*') existieren nicht als echte DE-Keys — das sind Platzhalter/Co |
| L6 | KEIN Plugin-Bug. Anmerkung zum eigenen Vorgehen: erste itertext-Variante mit ET.tostring+tail verdoppelte Tail-Text bei <xliff:g>-Strings (z.B. 'pro Monat pro Monat'); auf el.itertext() umgestellt -> sauber. Betraf nur meine Extraktion, NICHT die strings.xml (raw DE verifiziert korrekt). |
| L6 | Anmerkung: legal-keys-Liste enthaelt 2 Composite-Eintraege mit ' / ' (z.B. 'ai_prompt_insight_intro / profile_insight_*') die keine echten Einzel-Keys sind — als 93 echte Keys behandelt. |
| L7 | Die cross-lingual-matrix formatArgMismatch-Erkennung ist KORREKT und wertvoll (alle 7 Treffer echt), aber sie meldet nur 'de:[] vs loc:[%1$s]' ohne Hinweis auf die DAHINTERLIEGENDE strukturelle Divergenz (Laengen-Delta 1520 vs 4463 Zeichen). Vorschlag: bei formatArgMismatch zusaetzlich das Laengen-V |
| L7 | Die identicalToDe-Liste enthaelt JSON-Feldnamen (trend, datum, titel) und Marken (Premium, Dashboard), die per Definition identisch bleiben MUESSEN. Vorschlag: eine doNotTranslate-Allowlist (json_key_*, label_premium, brand-Strings, settings_about_version, month_* fuer germanische Sprachen) herausfi |
| L8 | Die cross-lingual-matrix formatArgMismatch-Erkennung verfehlt STALE-DRIFT ohne Argument-Unterschied: ai_prompt_rerank_system (pt-rBR, 459 vs 1864 Zeichen, veraltet) wurde als formatArgMismatch=0 gemeldet und waere ohne manuelle Laengenpruefung durchgerutscht. Vorschlag: pro Key zusaetzlich das Zeich |
| L8 | Die identicalToDe-Erkennung der Matrix uebersieht UNUEBERSETZTE PLURALS: datetime_months_relative + datetime_years_relative sind in pt-rBR/pt-rPT (und sehr wahrscheinlich weiteren Sprachen) rein deutsch, tauchen aber NICHT in identicalToDe auf, weil die Matrix Plurals separat behandelt (missingPlura |
| L8 | Die identicalToDe-Liste enthaelt erwartbar-identische Marken/Format-Labels (Premium, Foto, Foto %1$d, Best Journal V0.21.9), die per Definition gleich bleiben. Vorschlag (wie L7): eine doNotTranslate-Allowlist (label_premium, *_photo*, settings_about_version, brand-Strings) herausfiltern, damit iden |
| fa5 | {'hook': 'pretooluse-bash.sh', 'type': 'false-positive-on-readonly', 'detail': 'Inline `python -c` das eine geschuetzte Datei (auch nur lesend) referenziert wird blockiert. Read-only-Verifikation musste als separate .py-Datei laufen. Funktion des Guards ist korrekt (Schreibschutz), nur Read-Verifika |
| tw-en | {'symptom': "job-plan.json reports count=37 for 'en', but the strings array contains 35 keys (+2 plurals = 37 total entries). The literal string 'strings' count (35) differs from the headline 'count' (37).", 'evidence': "len(strings)=35, len(plurals)=2; my prompt said '37 strings inkl. ... + 2 plura |
| tw-en | {'symptom': 'Most target keys ALREADY existed in values-en/strings.xml holding stale/German content (incl. the 2 plurals which were still German); only paywall_monthly_note was genuinely new.', 'evidence': 'Pre-check grep showed 34 existing string keys =1 and paywall_monthly_note=0; plurals datetime |
| tw-es | {'symptom': "check_apostrophes.py requires --locale and --path flags; template (FIN-044) documents positional path-only usage 'check_apostrophes.py <path>'", 'evidence': "Running positional form errors: 'the following arguments are required: --locale, --path'", 'suggestion': 'Update translation-work |
| phase3-tw-gu.json | {'symptom': 'FIN-032 verlangt Skill(uebersetzung) pro Sprache, aber dieser Worker IST der vom uebersetzung-Skill gespawnte Translation-Worker — ein erneuter Skill-Aufruf von innen waere zirkulaer.', 'evidence': 'translation-worker.md Abschnitt 5 vs. tatsaechliche Spawn-Hierarchie (Skill -> Worker).' |
| roentgen-R1.json | app-roentgen-export.json enthält laut Aufgabenbeschreibung bereits Permissions/SDKs, war aber leer/nicht auswertbar für Dependency-Daten — der Exporter sollte libs.versions.toml bereits vorparsen, damit Worker R1 die TOML nicht selbst lesen muss. |
| roentgen-R1.json | Das Report-Schema für exportedComponents sollte den vollen Intent-Filter-Action-String abbilden (nicht nur exported=true/false) — für DSGVO-Audit ist der konkrete System-Broadcast entscheidend. |
| roentgen-R5.json | Kein Plugin-Bug waehrend dieser Ausfuehrung beobachtet. Token-Disziplin FIN-048 eingehalten: keine Datei vollstaendig gelesen, nur gezielte Greps + Read mit Offset/Limit-Parametern. |
| phase3-tw-fr.json | {'symptom': "NBSP/NNBSP fuer franzoesische Typografie zunaechst als literale \\u00A0/\\u202F-Escape-Strings in die Daten-JSON geschrieben; ein naiver raw-text .replace('\\\\u00A0', char) liess einen Backslash verwaist und zerstoerte die JSON (Invalid \\escape).", 'evidence': "JSONDecodeError line 34 |
| tw-hi | {'symptom': "pretooluse-bash hook blocked 'python3 -c' whose inline code STRING referenced app/.../strings.xml path", 'evidence': '[finale] BLOCKIERT: Scripting-Sprache mit Code-Argument das eine geschuetzte Datei referenziert', 'suggestion': 'Erwartetes Verhalten per Brief (Python als .py via Write |
| tw-hi | {'symptom': 'Python double-quoted string data file broke on escaped-quote sequence \\\\\\" (\\\\ + closing quote)', 'evidence': 'SyntaxError: unexpected character after line continuation character', 'suggestion': 'Translation payloads with embedded \\" and \\n are far safer as a .json data file than |
| tw-it | {'symptom': 'apply-Skript scheiterte zuerst mit ModuleNotFoundError, weil die Daten-Datei einen Bindestrich im Namen hat (it-data.py) und Python keine Module mit Bindestrich importieren kann.', 'evidence': 'from it_data import ... -> ModuleNotFoundError; Datei heisst it-data.py', 'suggestion': 'Work |
| tw-ja | {'symptom': 'pretooluse-bash.sh blockierte read-only inline `python3 -c` das die geschuetzte values-ja/strings.xml nur LIEST (zum Terminologie-Abgleich)', 'evidence': "Block-Text: 'Scripting-Sprache mit Code-Argument das eine geschuetzte Datei referenziert. Auch indirekte Schreibversuche...'", 'sugg |
| tw-ja | {'symptom': 'Erster `python3 -c` Dump crashte mit UnicodeEncodeError (cp1252) beim PRINTEN des DE-Texts mit ✓-Zeichen, obwohl PYTHONIOENCODING noch nicht gesetzt war', 'evidence': 'FIN-041 verlangt PYTHONIOENCODING=utf-8 VOR dem ersten Python-Aufruf; bug-case-Match 86%', 'suggestion': 'Daten waren k |
| phase3-tw-kn.json | {'symptom': "job-plan.json kn: Feld 'count' ist 37, aber das 'strings'-Array enthaelt 35 Eintraege (+2 plurals = 37). Gleiches Muster bei en/es (count 37, 35 strings). 'count' = strings+plurals, nicht = len(strings).", 'evidence': 'kn strings list len=35, plurals len=2, count=37; en/es identisch', ' |
| phase3-tw-kn.json | {'symptom': "Mehrere Bestands-Strings in values-kn waren bereits vorhanden, aber inhaltlich gegenueber dem aktuellen DE veraltet (z.B. privacy_gate_tts_title, settings_revoke_*). Ein reiner 'skip if present'-Ansatz (FIN-040 3.1) haette diese stale-Werte stehen lassen.", 'evidence': "privacy_gate_tts |
| phase3-tw-ko.json | {'symptom': 'Bestand values-ko mischt 해요체/합니다체 inkonsistent (Audit X12) — z.B. delete/revoke-Bodies waren 해요체, consent_do_not_sell war 합니다체', 'evidence': 'Nachbar-Key-Pruefung: settings_delete_account_confirm_body endete auf 삭제해요, consent_toggle_do_not_sell_body auf 남습니다', 'suggestion': "Register-Re |
| phase3-tw-ko.json | {'symptom': "settings_revoke_subtitle im Bestand war inhaltlich falsch ('Premium 구매' statt §356a-Widerruf)", 'evidence': "DE-Quelle: '§ 356a BGB — Widerruf direkt per App'; ko-Bestand: 'Premium 구매'", 'suggestion': "Stale/falsche Bestandswerte bei Rechts-Keys treten haeufiger auf — Pre-Audit koennte  |
| phase3-tw-ml.json | {'symptom': 'apply_ml.py crashed with re.PatternError: bad escape \\u at position 49 when translated values contained backslash escapes (e.g. \\u201e in settings_revoke_confirm_body)', 'evidence': "re.sub('...', repl_string, ...) interprets backslash sequences in the REPLACEMENT string as regex esca |
| phase3-tw-pl.json | {'symptom': 'pretooluse-bash.sh blockierte einen reinen READ-ONLY-Check (python3 -c mit grep-subprocess auf values-pl), weil der Code-String den geschuetzten Dateipfad als Literal enthielt.', 'evidence': 'Befehl rief nur grep -c auf, schrieb nichts; Guard matcht aber jede Scripting-Sprache + geschue |
| tw-ptbr | {'symptom': "finale pretooluse-bash.sh blockiert JEDES inline 'python3 -c' das einen geschuetzten Pfad (auch nur READEND, z.B. ET.parse zum Inspizieren) referenziert. Auch reine Lese-Skripte zum Verifizieren werden geblockt.", 'evidence': 'Block-Meldung bei cp_ptbr-Checkpoint (Schreiben) UND bei rei |
| tw-ptbr | {'symptom': 'Vor-Befund bestaetigt: ai_prompt_rerank_system war pt-rBR ein 459-Zeichen-Stub mit ROHEN Newlines (echte \\n-Bytes) statt Literal-Escape — genau die Fehlerklasse die FIN-049/Auftrag (c) adressiert.', 'evidence': 'Roh-Scan vor dem Fix: contains raw newline? True, len 459. Nach Fix: len 2 |
| tw-ptpt | {'symptom': 'Inline-Python-Strings mit \\\\" (escaped quote) und JSON-Daten mit \\\\" brachen den Parser (SyntaxError bzw. JSONDecodeError) — 2 Anlaeufe verloren.', 'evidence': 'ai_prompt_*-Strings enthalten viele \\"...\\"-Zitate fuer Android; in Python-Source und in JSON muss das als \\\\\\" kodie |
| phase3-tw-ta.json | {'symptom': 'Inline-Python (`python3 -c`) zum Inspizieren der Ziel-strings.xml wurde vom pretooluse-bash-Hook blockiert (Scripting referenziert geschuetzte Datei), und ein .py mit dem Uebersetzungs-Dict als Python-Literal scheiterte an SyntaxError durch \\\\" -Sequenzen (Backslash + echtes Quote bee |
| tw-te | {'symptom': "job-plan 'count' field for te = 38, but strings[] array holds 36 keys + plurals[] holds 2; momentary mismatch when reconciling 'inkl. 38 strings' wording in the brief against actual array length", 'evidence': 'len(te.strings)=36, len(te.plurals)=2, sum=38 == count:38', 'suggestion': "Or |
| phase3-tw-th.json | {'symptom': "pretooluse-bash.sh blockierte inline 'python3 -c' das die geschuetzte strings.xml referenziert — auch fuer reine Lese-Inspektion (open(file).read()).", 'evidence': "Hook-Meldung: 'BLOCKIERT: Scripting-Sprache mit Code-Argument das eine geschuetzte Datei referenziert'. FIN-048 verlangt P |
| tw-tr | {'symptom': "Bestehende values-tr-Strings waren teils mit 'KI' statt dem Bestands-Term 'YZ' uebersetzt (z.B. profile_entropy_long, mehrere ai_prompt_*), parallel zu 55 'YZ'-Vorkommen — inkonsistente KI-Terminologie im selben File.", 'evidence': "grep: 55x 'YZ' vs. 'KI'-Substrings in profile_entropy_ |
| tw-tr | {'symptom': "Frische tr-Uebersetzungen enthielten 6 Strings mit nicht-escapten Apostrophen (Groq'a, ABD'nin, ABD'deki, 2'sinin, 23.04.'te etc.) — haetten den Android-Resource-Build gebrochen.", 'evidence': "Post-Verify meldete 6 UNESCAPED-apostrophe-Faelle; nach \\' -Escaping: offizieller check_apos |
| phase3-tw-uk.json | {'symptom': "PreToolUse-Bash-Hook blockiert inline 'python3 -c' das job-plan.json/strings.xml referenziert", 'evidence': "Erste Inspektion mit python3 -c wurde mit '[finale] BLOCKIERT: Scripting-Sprache mit Code-Argument das eine geschuetzte Datei referenziert' abgewiesen", 'suggestion': 'Korrektes  |
| phase3-tw-uk.json | {'symptom': "PostToolUseFailure-Hook matchte 67% auf einen .NET-Build-Bug (TerminalVoiceOverlay.csproj), Exit 1 kam aber nur von 'grep -c' ohne Treffer", 'evidence': 'verify_uk.py meldete RESULT: PASS, der nachgelagerte grep -c (0 russische Zeichen, gewollt) liefert Exit 1', 'suggestion': 'Harmloser |
| phase3-tw-zhcn.json | {'symptom': 'Verifier-Trad-Char-Set hatte False-Positive (示 als traditionell markiert, ist aber gueltig vereinfacht)', 'evidence': '示 erscheint in 显示/提示 — Standard-Simplified', 'suggestion': 'Trad-Char-Set kuratieren: nur eindeutig-traditionelle Zeichen aufnehmen, nicht solche die in beiden Schrifte |
| phase3-tw-zhtw.json | {'symptom': "Inline 'python -c' mit Dateireferenz auf strings.xml wurde vom pretooluse-bash-Hook blockiert, obwohl es nur ein READ-ONLY Duplikat-Check war (ET.parse, kein write).", 'evidence': '[finale] BLOCKIERT: Scripting-Sprache mit Code-Argument das eine geschuetzte Datei referenziert.', 'sugges |
| phase3-tw-zhtw.json | {'symptom': "33 von 34 Keys waren bereits in values-zh-rTW vorhanden, aber inhaltlich veraltet/falsch gegenueber dem aktuellen DE-Quelltext (z.B. settings_revoke_subtitle='Premium 購買' statt §356a BGB; settings_delete_account_* nannte 'Firebase 帳戶'/'Google 帳戶 loeschen' obwohl DE sagt Konto bleibt bes |
