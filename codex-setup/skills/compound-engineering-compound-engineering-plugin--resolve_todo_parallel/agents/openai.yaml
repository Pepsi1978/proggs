interface:
  display_name: "resolve_todo_parallel"
  short_description: "Resolve all pending CLI todos using parallel processing"
  default_prompt: "Use $compound-engineering-compound-engineering-plugin--resolve_todo_parallel for this task."

policy:
  allow_implicit_invocation: false
