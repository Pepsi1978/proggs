interface:
  display_name: "developer"
  short_description: "Use this agent when implementing tasks from task files with"
  default_prompt: "Use $sdd-context-engineering-kit--developer for this task."

policy:
  allow_implicit_invocation: false
