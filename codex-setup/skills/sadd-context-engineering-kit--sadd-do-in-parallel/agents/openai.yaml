interface:
  display_name: "sadd:do-in-parallel"
  short_description: "Launch multiple sub-agents in parallel to execute tasks across"
  default_prompt: "Use $sadd-context-engineering-kit--sadd-do-in-parallel for this task."

policy:
  allow_implicit_invocation: false
