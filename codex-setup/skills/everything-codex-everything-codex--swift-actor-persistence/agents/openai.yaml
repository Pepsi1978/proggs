interface:
  display_name: "swift-actor-persistence"
  short_description: "Thread-safe data persistence in Swift using actors — in-memory"
  default_prompt: "Use $everything-codex-everything-codex--swift-actor-persistence for this task."

policy:
  allow_implicit_invocation: false
