interface:
  display_name: "sadd:launch-sub-agent"
  short_description: "Launch an intelligent sub-agent with automatic model selection"
  default_prompt: "Use $sadd-context-engineering-kit--sadd-launch-sub-agent for this task."

policy:
  allow_implicit_invocation: false
