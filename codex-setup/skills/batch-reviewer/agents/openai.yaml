interface:
  display_name: "batch-reviewer"
  short_description: "Parallel review for large local changesets"
  default_prompt: "Use $batch-reviewer for this task."

policy:
  allow_implicit_invocation: true
