interface:
  display_name: "sadd:judge"
  short_description: "Launch a sub-agent judge to evaluate results produced in the"
  default_prompt: "Use $sadd-context-engineering-kit--sadd-judge for this task."

policy:
  allow_implicit_invocation: false
