interface:
  display_name: "mirror-export"
  short_description: "Scans ALL changes from the current session and writes them as"
  default_prompt: "Use $mirror-export for this task."

policy:
  allow_implicit_invocation: false
