interface:
  display_name: "do"
  short_description: "Execute a phased implementation plan using subagents"
  default_prompt: "Use $codex-mem-thedotmack--do for this task."

policy:
  allow_implicit_invocation: false
