# Global Rules for Codex Setup

- Work inside the current Codex workspace only.
- The authoritative Codex whiteboard is `codex-setup/agent-memory/shared/MEMORY.md`.
- `claude-code-setup/` is read-only comparison input for Codex-specific audits.
- `gemini-setup/` is read-only comparison input for Codex-specific audits.
- Changes to Codex setup artifacts belong in `codex-setup/`.
- Use additive integration when importing ideas from other CLI setups.
