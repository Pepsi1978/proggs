# Pipeline-Logbuch — Lauf 01

Stand: 09.08.2026 · Skill: `neue-applikation` (**Erstbenutzung**) · Projekt: *(Name noch offen)*
Geführt von: Claude Code (Opus 5) · Auftraggeber: Frank

---

## 1. Wozu dieses Logbuch

Es ist das erste Mal, dass die Pipeline `neue-applikation` (spec-schmiede → Werft Studio →
spec-rueckimport → design-umsetzer) durchlaufen wird. Dieses Logbuch hält fest:

- **was gemacht wurde** — Schritt für Schritt, mit Ergebnis
- **was schiefging** — jeder Fehler, jede Stolperstelle, jede Reibung, auch die kleinen
- **was an den Skills korrigiert werden muss** — konkrete Verbesserungsvorschläge

Es wird **nach jedem Schritt fortgeschrieben**, nicht erst am Ende.

---

## 2. Lauf-Übersicht

| # | Stufe | Skill | Status | Ergebnis |
|---|-------|-------|--------|----------|
| 0 | Klammer | `neue-applikation` | ✅ gestartet | Kette angekündigt |
| 1 | Stufe 1 | `spec-schmiede` | ✅ fertig | `Specs/Experimente/v1/` (4 Dateien) + `Designs/Inbox/Experimente-SPEC-v1.zip` (6 Einträge) |
| 2 | Hand | Werft Studio | ✅ fertig | `Designs/Outbox/Experimente-SPEC-v1-SPEC-v2.zip` (198 KB) — Vollausbau mit Messpaket |
| 3 | Stufe 2 | `spec-rueckimport` | ✅ fertig | `Specs/Experimente/v2/` (6 Dateien) inkl. `BAU-AUFTRAG.md` |
| 4 | Stufe 3 | `design-umsetzer` | ✅ fertig | `Experimente/` — 9 Bildschirme, Navigation, 46 Kotlin-Dateien, `assembleDebug` grün |
| 5 | Klammer | Build + Installation | ⚠️ halb | APK gebaut (18,9 MB, v0.2.0) — **Installation und optischer Abgleich stehen aus: kein Gerät an ADB** |

Legende: ⬜ offen · 🔄 läuft · ✅ fertig · ⚠️ mit Auffälligkeit · ❌ gescheitert

---

## 3. Verlauf

### 2026-08-09 — Start

**[V-01] Klammer gestartet.** Frank ruft `neue-applikation` mit einer langen gesprochenen
Rohidee auf (Android-App für persönliche Experimente). Klammer meldet den Ablauf und ruft
`spec-schmiede` auf. Ohne Reibung.

**[V-02] Phase 0 — Regeln gelesen.** `Specs/README.md` (v1.1.0) und `Specs/FORMAT.md` (v1.0.0)
gelesen. `Designs/Inbox/` und `Designs/Outbox/` existieren, beide leer. `Specs/` enthält nur
die beiden Regeldateien — kein Namenskonflikt zu erwarten.

**[V-03] Phase 0 — Verständnis zurückgespiegelt.** Zusammenfassung der Rohidee in
15 Zeilen vorgelegt. Frank bestätigt („soweit hast du das richtig verstanden") und ergänzt
umfangreich (siehe [V-04]).

**[V-04] Nachtrag durch Frank — deutliche Erweiterung der Vorgabe.** Neu hinzugekommen:
Erkenntnisliste, großer Selbstbild-Speicher in den Einstellungen, Definition „Experiment =
etwas noch nie Gemachtes", Steigerung von leicht nach fordernd, Chronik der Experimente,
Kategorien bei den Vorschlägen, Vorlesen per Sprachausgabe, eigene KI-Auswertung nach der
gesprochenen Auswertung. → siehe [B-01] in Abschnitt 5.

**[V-05] Dieses Logbuch angelegt** auf Franks ausdrücklichen Wunsch, noch während Stufe 1
läuft. Ort: `Specs/_Pipeline-Logbuch/2026-08-09-lauf-01.md`.

**[V-06] Zweiter Nachtrag von Frank.** Vorlesen der KI-Auswertung per Lautsprecher-Knopf,
Stimmen einstellbar (Chirp 3 HD **und** eigene Stimme, beides „schon in PerfectMoment
eingebaut, da kannst du nachgucken"), Whisper Large V3 Turbo für die Transkription,
Hell-/Dunkel-Modus.

**[V-07] PerfectMoment durchgesehen — alle Bausteine bestätigt.** `TtsCatalog.kt` kennt drei
Anbieter (`EDGE` = Microsoft Edge, Standard · `GOOGLE_CLOUD` = Google Chirp 3 HD mit 31
deutschen Stimmen · `QWEN_CLONE` = „Meine Stimme"). `GroqTranscriber.kt` nutzt
`whisper-large-v3-turbo` über `api.groq.com/openai/v1/audio/transcriptions`, Deutsch,
`temperature=0`, max 25 MB, mit `SpeechAnalyzer` als Vorfilter und
`WhisperHallucinationFilter` danach. `QwenVoiceEnrollment.kt` registriert die eigene Stimme
über Alibaba DashScope (`qwen-voice-enrollment` → `qwen3-tts-vc-2026-01-22`). Schlüssel und
Theme liegen in `SecureSettings.kt` (EncryptedSharedPreferences, AES-256).
→ **F-01 damit geklärt und geschlossen.**

**[V-08] Block A begonnen.**
- Name: **Experimente** (Franks Wahl; meine vier Vorschläge — Neuland, Selbstversuch,
  Versuchsfeld, Wagnis — verworfen). Kein Namenskonflikt in `Specs/`, `Designs/Inbox/`
  oder als Quellcode-Ordner.
- Plattform: Android, Kotlin + Jetpack Compose (abgeleitet, nicht gefragt — Repo-Konvention).
- Für wen: **nur für Frank selbst.** → Brillen *Erstbenutzung*, *Recht/Datenschutz*,
  *Barrierefreiheit* und *Mehrbenutzer* bleiben **aus**; `04-ONBOARDING-SPEC.md` und
  `05-RECHT-SPEC.md` entfallen.
- KI-Weg: **Codex-OAuth wie PerfectMoment** (ChatGPT-Abo, `gpt-5.6-luna/-sol/-terra`).
  Alternativen mit nachgeschlagenen Preisen vorgelegt (Claude Sonnet 5 ≈ 3 €/Monat,
  Claude Opus 5 ≈ 8 €/Monat) und von Frank verworfen.

**[V-09] Durchgang 1 begonnen — ohne Subagenten.** Der Skill sieht parallele Prüf-Agenten
(Funktion / Aussehen / Bewegung / Widersprüche) plus Operator vor. Diese Sitzung hat die
Agent-Werkzeuge abgeschaltet („Do not call the AgentTool unless the user requested it"),
also wurden alle vier Brillen und der Operator **inline** angewandt. Die Prüfung ist
erfolgt, nur nicht parallel. → siehe [B-03] in Abschnitt 5.

**[V-10] Operator-Fund: Widerspruch im Fundament.** „Nur die letzten 30 Tage speichern"
stand gegen „Experiment = etwas noch nie Gemachtes" und gegen die geforderte Chronik —
nach 30 Tagen hätte die App bereits Gemachtes erneut vorgeschlagen. Frank hat daraufhin die
Speicher-Landschaft neu geordnet.

**[V-11] Speicher-Landschaft entschieden** (Franks Vorgabe, von mir zu Zahlen verdichtet):

| Speicher | Lebensdauer | Umfang je Tag | Wer schreibt |
|----------|-------------|---------------|--------------|
| Selbstbild | dauerhaft | beliebig | Frank (Einstellungen) |
| Aktuelles Log | 15 Tage rollierend | 20–30 Zeilen, ausführlich | KI |
| Langzeit-Log (= Chronik) | dauerhaft | max. 6–7 Zeilen, stichpunktartig | KI |
| Erkenntnisliste | dauerhaft | — | KI |
| Wunschliste | bis abgearbeitet | — | Frank + automatisch |

Kernmechanik: Nach 15 Tagen **wandert** ein Tag ins Langzeit-Log und wird dabei von ~25 auf
~6 Zeilen verdichtet — er fällt nicht heraus. Von 30 auf 15 Tage heruntergesetzt, weil 30
ausführliche Tage zu viel Einlese-Kontext wären (Franks Begründung).
„Chronik" und „Langzeit-Log" wurden als **ein** Speicher zusammengeführt.

**[V-12] Nicht umgesetztes Experiment: beide Wege.** Es wird ausgewertet (die KI lernt,
warum es nicht kam) **und** wandert zurück auf die Wunschliste; beim Wiedervorschlagen
bringt es das Gelernte mit.

**[V-13] Gesprächsbereich neu aufgenommen.** Nach der Auswahl kann Frank mit der KI über das
Experiment sprechen — fortlaufend, Antwort wird sofort vorgelesen. Vorlage: die eigene
Cortex-App (`ui/chat/ChatScreen.kt` + `ChatViewModel.kt`, Whisper rein / TTS raus).
Entscheidung: das Gespräch **hängt am Experiment**, es wird nicht der Startbildschirm —
der Tagesrhythmus bleibt das Rückgrat.

**[V-15] Wunschliste-Mechanik entschieden.** Liegt etwas darauf, ist **einer der fünf**
Tagesvorschläge von dort und wird als solcher gekennzeichnet. Garantiert, dass die Liste
abgearbeitet wird, statt zu wachsen.

**[V-16] Erinnerungen: morgens und abends**, Zeiten in den Einstellungen, einzeln
abschaltbar. Braucht `POST_NOTIFICATIONS` (Android 13+); bei Ablehnung läuft die App wie
ohne Erinnerungen weiter.

**[V-17] Bildschirmliste abgeleitet und vorgelegt** (nicht abgefragt — Block-D-Verfahren).
Dabei zwei Zusammenlegungen vorgeschlagen: „Chronik" = „Langzeit-Log" (ein Bildschirm), und
die fünf Vorschläge sind ein **Zustand** von *Heute*, kein eigener Bildschirm.

**[V-18] Vierter Nachtrag: Bereich „Wünsche & Ziele".** Franks persönliche Lebensziele,
selbst eingegeben, mehrere hintereinander, per Sprache **mit KI-Verbesserung**. Die
Experimente sollen schrittweise darauf hinarbeiten. Nachgesehen: `improveWish()` in
`CodexAuthManager.kt` (merkt alle bisherigen Fassungen → jeder Druck liefert eine neue
Formulierung), Knopf heißt in PerfectMoment „Text mit KI verbessern", mit Rücknahme.
KI-Verbesserung gilt auch für die **Situationseingabe** — womit das Transkript doch vor dem
Absenden sichtbar wird und der Einwand aus [V-06] gelöst ist.

**[V-19] Widerspruchs-Fund: Namenskollision.** „Wunschliste" (aufgehobene Experimente) und
„Wünsche & Ziele" (Lebensziele) sind zwei verschiedene Dinge mit demselben Wort.
Entscheidung: **Merkliste** für die aufgehobenen Experimente, **Wünsche & Ziele** für die
Lebensziele.

**[V-20] Mehrtägige Experimente.** Franks Laufbeispiel spannte über zwei Tage und passte
nicht in „ein Experiment, ein Tag" — beim Nachfragen erweitert auf: mehrtägige Experimente
sind möglich, **höchstens drei gleichzeitig offen**. Abgeleitet und bestätigt: bei drei
offenen kommen keine neuen Vorschläge; die Dauer schlägt die KI mit vor. Der Abend zeigt
**alle offenen der Reihe nach** — Zwischentage bekommen einen Zwischenstand, die volle
KI-Auswertung erst am letzten Tag.

**[V-14] Zwei Modellwahlen + Effort bestätigt.** `CodexModels.kt` liefert drei Modelle
(Sol, Terra, Luna) und fünf Effort-Stufen (Niedrig, Mittel, Hoch, Sehr hoch, Maximal).
In den Einstellungen je eine Wahl für **Experimente** und eine fürs **Logbuch**.

**[V-21] Fünfter Nachtrag mitten in der Durchgangsfrage: To-Do-Liste.** Auf Nachfrage
präzisiert: **eine** Liste für den Tag, nicht eine je Experiment — Experimenttitel als
Zwischenüberschrift, darunter dessen heutige Aufgaben, dann der nächste Block. Haken zählen
für die KI-Auswertung; Franks eigene gesprochene Auswertung bleibt trotzdem Pflicht.

**[V-22] Sechster Nachtrag: keine festen Lebensbereiche.** Mein Vorschlag von sechs
Bereichen mit Farben wurde verworfen — „ein Mensch besteht nicht aus sechs Bereichen".
Stattdessen zieht die KI frei aus allem. Dafür kam eine **exakte Zusammensetzung der fünf**:
2 zur Lage passend · 2 völlig neu · 1 von der Merkliste. Merkliste bekommt einen Plus-Knopf
für eigene Ideen.

**[V-23] Durchgang 2 — Aussehen und Bewegung.** Zuerst Cortex und PerfectMoment
durchgesehen (`ui/theme/Color.kt`, `Type.kt`, `DESIGN-BRIEF.md`). Cortex hat eine fertige
Design-Sprache; Entscheidung gegen deren Übernahme, weil Cortex ein kühles Nachschlagewerk
ist und diese App eine andere Aufgabe hat. Grundhaltung: **ruhig und warm im Grundton, klar
und sachlich im Aufbau** (Franks Mischung aus meinen Optionen 1 und 3). Vollständige Palette,
Typografie (Fraunces / Inter / JetBrains Mono), Formensprache und ein vollständiges
Motion-Paket vorgelegt und angenommen.

**[V-24] Schlussdurchgang.** Gegen alle drei Specs geprüft. Drei Kleinteile selbst
entschieden und im Spec kenntlich gemacht (Erkenntnis-Schreibzeitpunkt, Verdichtungs-Zeitpunkt,
Liste der Fehlerfälle). Eine echte Lücke gefunden und gefragt: die **Dosierung** von
„herausfordern, aber nicht überfordern" — Ergebnis: die KI dosiert aus der Chronik, die Stufe
(leicht · mittel · fordernd) steht sichtbar auf jeder Karte.

**[V-25] Spec-Paket geschrieben.** `Specs/Experimente/v1/` mit vier Dateien (00-PROJEKT,
01-FUNKTIONS, 02-UI, 03-MOTION). Kein `04-ONBOARDING` und kein `05-RECHT` — die
Zusatz-Brillen waren aus. 26 Funktionen (F-01…F-26), 9 Bildschirme (B-01…B-09), 9 Bewegungen
(M-01…M-09), 20 Abnahmekriterien (A-01…A-20).
`Designs/Inbox/Experimente-SPEC-v1.zip` mit **6 Einträgen**, flach, 53 KB — Inhalt geprüft.
Die Teile A–C in `SPEC.md` wurden **maschinell** aus den Einzeldateien zusammengesetzt
(`cat`), nicht abgetippt, damit Übergabedatei und `Specs/` nicht auseinanderlaufen können.

**[V-26] Stufe 2 — Rückimport.** Rücklauf kam als **Vollausbau**: drei fortgeschriebene
Spec-Dateien **und** das Messpaket `WERFT-DESIGN/` (30 Dateien, 9 Bildschirme × 2
Erscheinungen). Abgleich mechanisch, Kennung für Kennung.

**[V-27] Befund: Bildschirme und Abnahme deckungsgleich.** Alle 9 `B-` und alle 20 `A-`
erhalten, keiner entfallen, keiner neu. **Alle 26 gemessenen Farbwerte exakt identisch mit
der v1-Absicht** — Werft hat spec-treu gebaut.

**[V-28] Befund: `F-27` doppelt vergeben.** Werft nutzte `F-27` in der Überblickstabelle für
die Wisch-Navigation und in der Elementtabelle für den Erscheinungs-Schnellschalter.
Aufgelöst: `F-27` = Wisch-Navigation (die einzige wirklich neue Funktion). Die übrigen sechs
gemeldeten Elemente (`F-28`…`F-33`) sind **keine neuen Funktionen** — sie standen bereits im
v1-UI-Spec, dort nur als Fließtext ohne eigene Kennung. Sie wurden an `F-01`, `F-10`, `F-19`,
`F-26` bzw. `B-07` gehängt; `F-28`…`F-33` bleiben unvergeben.

**[V-29] `F-33` ohne Rückfrage gelöst.** Werft hatte „Abbrechen" auf der Merkliste als
`OFFEN — beim Rückimport klären` markiert. Das Markup beantwortet es eindeutig: Der Knopf
steht im `<dialog class="b05-dialog">` („Eintrag löschen?") neben einem Knopf mit
`data-werft-funktion="F-19"`. Abgelesen, nicht erfunden — deshalb keine Frage an Frank.

**[V-30] Befund: vier Bewegungen fehlten — Messlücke, kein Designer-Entscheid.** `M-01`
(Druck-Einsinken), `M-03` (Vibration), `M-05` (zweiphasiger Kartentausch) und `M-06`
(Haken zeichnet sich) kamen im Rücklauf nicht vor, weil ein statischer HTML-Export weder
`:active`-Zustände noch Haptik noch gesteuerte Abfolgen noch SVG-Pfadanimationen tragen kann.
Nach der Regel „Design gewinnt" wären sie stillschweigend aus der App gefallen — Frank
vorgelegt, Entscheidung: **aus v1 zurückholen**, mit ausdrücklichem Quellenvermerk.

**[V-31] Befund: 77 Bewegungseinträge unter nur 71 Kennungen.** `M-02` sechsmal, `M-08`
zweimal — dieselbe Bewegung, in mehreren CSS-Regeln gemessen. Zusammengeführt zu je einem
Eintrag mit Liste aller Fundstellen. **Keine Umnummerierung.** Ohne das wäre die Abhakliste
im Bau-Auftrag wertlos gewesen.

**[V-32] Befund: §7 und §8 fehlten im gelieferten UI-Spec.** Die Gliederung sprang von §6
direkt zu §9. Ikonografie und Texte aus v1 zurückgeholt — das Messpaket schweigt dazu
(`assets` und `texte` sind leere Listen).

**[V-33] Aufräumung ohne Wertänderung.** Werft hatte an 49 Stellen einen internen Pfad
(`.werft-generated/019fe79c-…/1/design.html`) als **Rollennamen** in den Tabellen §3–§5
eingesetzt. Auf `design.html` gekürzt. Kein gemessener Wert angefasst.

**[V-34] v2 geschrieben und nachgewiesen.** 6 Dateien. 9 Bildschirme · 27 Funktionen ·
75 Bewegungen · 20 Abnahmekriterien, alle Kennungen eindeutig, beide Erscheinungen
vollständig, alle 8 App-`@keyframes` abgedeckt, Werfts Vorschau-Animationen ausgeschlossen.
**`v1` blieb unangetastet** (per `git status` geprüft). Keine offenen Fragen.

### 2026-08-09 — Stufe 3: der Bau

**[V-35] Bau in sechs Blöcken.** Fundament/Theme → Datenschicht → Dienste → KI-Aufgaben und
Ablage → die neun Bildschirme → Navigation. Nach jedem Block ein `compileDebugKotlin`; der
Lauf war durchgehend grün. Am Ende 46 Kotlin-Dateien, `assembleDebug` grün, APK 18,9 MB.

**[V-36] Lücke im Funktions-Spec geschlossen: die Einheit „Lage" fehlte.** §3 des
Funktions-Specs listet zehn Einheiten, F-01 schreibt aber eine `SituationEntry`, die dort
nicht steht. Eine elfte Einheit `Lage` (Datum + Text) wurde angelegt. Das Spec selbst wurde
**nicht** geändert — die Lücke steht hier.

**[V-37] Fehlende Kurve im Motion-Spec-Token-Satz.** M-08 („die KI-Antwort erscheint",
400 ms `ease-out` → `cubic-bezier(0, 0, 0.58, 1)`) hatte in `Motion.kt` kein Gegenstück; die
vorhandene 400-ms-Kurve `weich` ist eine andere (`0.4, 0, 0.2, 1`). Als `Bewegung.antwort`
ergänzt, weil M-08 auf **zwei** Bildschirmen läuft (B-02 Blase, B-03 Auswertungskarte).

**[V-38] Design ohne Bedienelement, Spec mit Pflicht: der Lautsprecher auf B-02.** Der
Fehlerfall von F-09 verlangt ausdrücklich *„ein Lautsprecher-Knopf erlaubt einen neuen
Versuch"*, wenn das Vorlesen scheitert. Im Design der Gesprächsblase gibt es keinen. Auf
B-03 hat der Designer einen Lautsprecher vorgesehen, auf B-02 nicht. Entschieden: Der
Lautsprecher wurde in die KI-Blase aufgenommen — Aussehen folgt dem Design, **fehlende
Spec-Funktionen werden ergänzt**.

**[V-39] „Nicht umgesetzt" (F-13) saß im Design an einer anderen Stelle als vermutet.** Nicht
auf B-03, sondern auf der Experimentkarte von **B-01** (`.werft-b01__text-action`,
`data-werft-funktion="F-13"`). Der erste Durchgang an B-01 hatte den Knopf übersehen; er
wurde nachgetragen und führt auf B-03 mit gesetztem Merker.

**[V-40] Zwei geteilte Bauteile an die einzige Design-Quelle angeglichen.**
`Wartezustand` und `Fehlerkarte` kommen im ganzen Design **nur** auf B-03 vor. Danach
korrigiert: Wartetext 20 dp statt 12 dp unter dem Balken, Fehlertext in *Warnung* statt in
*Text*, „Nochmal versuchen" 12 dp statt 8 dp Abstand.

**[V-41] Layout-Fehler, den der Compiler nicht sieht.** Der Plus-Knopf von B-04/B-05 lag als
zweites `fillMaxSize`-Kind in derselben Spalte wie der Inhalt und hätte damit die Höhe 0
bekommen — grün kompiliert, unsichtbar zur Laufzeit. Auf ein `Box`-Gerüst mit
`align(BottomEnd)` umgestellt. Merkposten: **Grün ist kein Nachweis für Layout.**

**[V-42] Das Design schweigt zu den Listenzeilen von B-06 und B-07.** Beide Bildschirme
zeigen im Entwurf ausschließlich den leeren Zustand; es gibt keine Vorlage für einen
Erkenntnis- oder Logbuch-Eintrag. Gebaut wurde in der Kartensprache der App
(`Karte` + Datum in `daten` + Text in `fliesstext`) — hier ist der Bau also **nicht** durch
das Design gedeckt.

**[V-43] Zwei bewusst offen gelassene Punkte.**
1. F-23 Schritt 3 verlangt *„bestehende Stimmen verwalten und löschen"*. Das Design hat für
   „Meine Stimme" nur **einen** Knopf („Stimme aufnehmen"). Umgesetzt sind Aufnahme und
   Registrierung; eine Verwaltungsliste wurde **nicht** erfunden.
2. Der Beispielsatz für „Probe hören" steht in keinem Spec-Abschnitt. Gesetzt wurde
   *„So klinge ich, wenn ich dir etwas vorlese."*

**[V-44] Bildschirmwechsel nach M-63/M-64.** Zwischen den fünf Hauptbildschirmen reines
Überblenden 200 ms (`blenden`), tiefer hinein Hereinschieben von rechts 240 ms (`ruhig`).
F-27 (Wischen) ohne Pager gebaut, damit der Wechsel überblendet statt mitzuziehen — ein
Pager hätte das Schieben erzwungen und M-63 widersprochen.

**[V-45] Version gehoben und gebaut.** `versionCode` 1 → 2, `versionName` 0.1.0 → 0.2.0,
`VERSION_BUMPED_AT` auf den 09.08.2026, 23:59 Uhr. `assembleDebug` grün,
`testDebugUnitTest` grün (es gibt noch keine Testklassen).

**[V-46] Installation nicht möglich.** `adb devices` ist auch am Ende des Baus leer. Damit
entfallen: die Installation aufs Gerät **und** der vom Skill verlangte Screenshot-Abgleich je
Bildschirm und je Erscheinung. Die Umsetzung gilt bis dahin als **optisch ungeprüft**.

---

## 4. Fehler und Stolperstellen

| Nr | Wo | Was passiert ist | Auswirkung | Status |
|----|----|------------------|------------|--------|
| F-01 | Rohidee | Spracherkennung gab „CROC Large V3 Turbo" bzw. „Visbalage V3 Turbo" aus; gemeint war **Whisper Large V3 Turbo**. Ebenso „SHIRP3 HD" → **Chirp 3 HD** | keine — durch Nachsehen in PerfectMoment eindeutig geklärt | ✅ erledigt |
| F-02 | Umgebung | `adb devices` liefert **kein angeschlossenes Gerät** | Installation und Screenshot-Abgleich entfallen — Umsetzung bleibt optisch ungeprüft | ❌ offen, blockiert Schritt 5 |
| F-03 | Stufe 3, B-04/B-05 | Plus-Knopf als zweites `fillMaxSize`-Kind einer Spalte → Höhe 0, kompiliert aber grün | wäre zur Laufzeit unsichtbar gewesen | ✅ erledigt (V-41) |
| F-04 | Stufe 3, B-01 | „Nicht umgesetzt" (F-13) im ersten Durchgang übersehen — der Knopf steht im Design auf der Experimentkarte von B-01, nicht auf B-03 | F-13 wäre ohne Auslöser geblieben | ✅ erledigt (V-39) |
| F-05 | Session | Token-Grenze mitten in Block 5; Fortsetzung über `session backup` → `/clear` → `session restore` | keine — der Wiedereinstiegspunkt trug, der unterbrochene Compile-Lauf wurde als erste Handlung nachgeholt | ✅ erledigt |

---

## 5. Beobachtungen und Verbesserungsvorschläge für die Skills

| Nr | Skill | Beobachtung | Vorschlag | Priorität |
|----|-------|-------------|-----------|-----------|
| B-01 | `spec-schmiede` | Phase 0 sieht **eine** Rückspiegelung vor und geht danach zu Block A. Franks Bestätigung kam aber zusammen mit einer Erweiterung, die den Umfang der App etwa verdoppelt hat. Der Skill hat für diesen Fall keine Regel — er würde mit einer veralteten Vorgabe in Block A gehen. | In Phase 0 eine Schleife vorsehen: *„Kommt mit der Bestätigung neue Substanz, wird erneut zurückgespiegelt — so lange, bis die Bestätigung ohne Nachtrag kommt."* | hoch |
| B-03 | `spec-schmiede` | Der Skill schreibt parallele Prüf-Agenten je Durchgang vor. Sind Subagenten in der Sitzung abgeschaltet, hat er dafür keinen Ersatzweg — wortwörtlich befolgt müsste der Skill hier anhalten. | Rückfallebene aufnehmen: *„Stehen keine Subagenten zur Verfügung, setzt du die Brillen nacheinander selbst auf und hältst das im Logbuch fest. Die Prüfung entfällt nie, nur ihre Parallelität."* | mittel |
| B-04 | `spec-schmiede` | Die Rückspiegelung kam dreimal mit einem Nachtrag zurück, der den Umfang jeweils spürbar erweiterte (Erkenntnisliste/Selbstbild → Stimmen/Transkription → Log-Architektur/Gespräch). Der Skill behandelt Phase 0 als einmaligen Schritt. | Bestätigt B-01 aus der Praxis: Die Rückspiegelung braucht eine ausdrückliche Schleife, sonst arbeitet Block A gegen einen veralteten Stand. | hoch |
| B-05 | `Specs/README.md`, `spec-rueckimport` | **Werft benennt den Rücklauf anders als erwartet.** Erwartet: `<App>-SPEC-v2.zip`. Tatsächlich geliefert: `Experimente-SPEC-v1-SPEC-v2.zip` — Werft hängt `-SPEC-v2` an den **Importnamen** an, der bereits `-SPEC-v1` enthielt. Bei jedem weiteren Durchlauf wächst der Name weiter (`…-v1-SPEC-v2-SPEC-v3.zip`). | Die Erwartung in `Specs/README.md` und in `spec-rueckimport` auf ein **Muster** umstellen statt auf einen festen Namen: `Experimente*v2*.zip`. Zusätzlich in `LIESMICH.md` klarstellen, dass der Rücklauf umbenannt werden darf. | mittel |
| B-06 | `neue-applikation` | Das Übergabe-ZIP in `Designs/Inbox/` ist per `.gitignore` von der Versionierung ausgenommen. Das ist richtig (Briefkasten, kein Archiv) — aber der Skill sagt „committen und pushen", ohne zu erwähnen, dass ein Teil des Ergebnisses bewusst nicht mitgeht. | Einen Satz ergänzen: *„Das ZIP in `Designs/Inbox/` wird nicht versioniert — die Wahrheit liegt in `Specs/<App>/v1/`. Das ZIP lässt sich daraus jederzeit neu bauen."* | niedrig |
| B-07 | `design-umsetzer` | Der Skill verlangt einen **Screenshot-Abgleich je Bildschirm und je Erscheinung** — und kennt keinen Weg, wenn kein Gerät angeschlossen ist. Wortwörtlich befolgt endet der Skill damit ohne Abschluss. | Rückfallebene aufnehmen: *„Ohne Gerät wird gebaut und ausdrücklich als ‚optisch ungeprüft' gemeldet; der Abgleich wird als offener Punkt ins Logbuch geschrieben und beim nächsten angeschlossenen Gerät nachgeholt."* Optional den Emulator als zweiten Weg nennen. | hoch |
| B-08 | `spec-rueckimport` | Die Bewegungslisten je Bildschirm im UI-Spec enthalten Werfts **eigene Vorschau-Animationen** (M-50 bis M-75 tauchen auf jedem Bildschirm auf). Die wirklich bildschirmeigenen Bewegungen (M-08, M-02) gehen darin unter — beim Bau muss man jede Liste erst von Hand sieben. | Beim Rückimport zweiteilen: *„Bewegungen dieses Bildschirms"* und *„Bewegungen der Werft-Vorschau (nicht bauen)"*. Die Zuordnung ist maschinell möglich (Quelle `werft-screen-*`). | mittel |
| B-09 | `spec-rueckimport` | Zeigt ein Entwurf nur den **leeren Zustand** (B-06, B-07), enthält das UI-Spec keine Vorlage für die gefüllte Liste. Der Bau muss die Zeilenform erfinden — genau die Stelle, an der „1:1 umsetzen" nicht mehr prüfbar ist. | Beim Rückimport ausdrücklich vermerken: *„Für ⟨Bildschirm⟩ liegt keine gefüllte Vorlage vor — die Zeilenform ist frei und muss abgenommen werden."* So steht die Lücke im Bau-Auftrag statt im Nachhinein im Logbuch. | mittel |
| B-10 | `design-umsetzer` | An zwei Stellen verlangt das Funktions-Spec ein Bedienelement, das im Design fehlt (Lautsprecher auf B-02, Stimmenverwaltung auf B-08). Der Skill sagt „Design gewinnt beim Aussehen", trifft aber keine Aussage zur umgekehrten Richtung. | Regel ergänzen: *„Fehlt ein vom Funktions-Spec verlangtes Bedienelement im Design, wird es in der Formsprache der App ergänzt und im Logbuch als Abweichung festgehalten. Erfunden wird nie ein ganzer Bereich — der bleibt offen."* | hoch |
| B-02 | `neue-applikation` | Der Skill sieht kein Lauf-Logbuch vor. Frank musste es ausdrücklich anfordern. Bei einer Pipeline über vier Stufen mit einem Handgriff dazwischen ist ein Protokoll aber die einzige Möglichkeit, hinterher zu sagen, wo es geklemmt hat. | Logbuch als festen Bestandteil aufnehmen: bei Schritt 0 anlegen, nach jedem Schritt fortschreiben, am Ende als Auswertung vorlegen. | hoch |

---

## 6. Offene Punkte

- App-Name steht noch nicht fest → sobald er da ist, hier und im Kopf der Datei nachtragen.
