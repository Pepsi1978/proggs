<Window x:Class="ClaudeVoiceOverlay.Views.GeminiPromptListDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gemini-Prompts"
        Width="480" Height="600"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        ResizeMode="NoResize">
    <Border CornerRadius="16"
            Background="#FF1E1E1E"
            BorderBrush="#FF3A3A3A"
            BorderThickness="1"
            Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Gemini-Prompts bearbeiten"
                       Foreground="#FFEEEEEE"
                       FontWeight="Bold"
                       FontSize="14"
                       Margin="0,0,0,8"/>

            <TextBlock Grid.Row="1" Foreground="#FF9A9A9A" FontSize="11" TextWrapping="Wrap" Margin="0,0,0,12"
                       Text="Wähle ein Profil zum Bearbeiten. Jedes Profil hat seine eigene Vorlage, die an Gemini geschickt wird. Die Prompts gelten für alle Overlays (Terminal und Claude) auf diesem Rechner."/>

            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ProfileList"/>
            </ScrollViewer>

            <Button Grid.Row="3" x:Name="BtnClose"
                    Content="Schließen"
                    HorizontalAlignment="Right"
                    Background="#FF2D2D2D" Foreground="#FFCCCCCC"
                    BorderBrush="#FF454545" BorderThickness="1"
                    Padding="16,6" Margin="0,12,0,0" Cursor="Hand"/>
        </Grid>
    </Border>
</Window>
