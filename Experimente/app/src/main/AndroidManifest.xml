<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
        E-23 (Haptik): feste Rüttelmuster beim Abhaken, Aufnehmen, Starten und Abschließen.
        Ohne diese Erlaubnis wirft `Vibrator.vibrate()` eine SecurityException — sie ist eine
        normale Berechtigung, sie wird nicht zur Laufzeit erfragt.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
        F-12 (Vorlesen): der LoudnessEnhancer hebt die Sprachausgabe um 12 dB an. Die rohen
        Sprachdateien der Anbieter liegen deutlich unter dem Pegel, den ein Telefonlautsprecher
        braucht — ohne diese Erlaubnis bleibt der Effekt aus und das Vorlesen ist kaum hörbar.
    -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".ExperimenteApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Experimente">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Experimente">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- F-25: Erinnerungen morgens und abends -->
        <receiver
            android:name=".notify.ReminderReceiver"
            android:exported="false" />

        <!-- Weckzeiten nach einem Neustart des Geraets neu setzen -->
        <receiver
            android:name=".notify.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
