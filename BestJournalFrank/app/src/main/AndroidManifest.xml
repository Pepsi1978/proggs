<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <queries>
        <package android:name="com.google.android.gms" />
    </queries>

    <!-- Frank-Wunsch 2026-05-24: Leseberechtigung fuer die Tagebuch-Durchreiche.
         protectionLevel "normal", weil Entropie Reductor mit anderem Signatur-Key
         signiert ist (kein signature-Schutz moeglich). -->
    <permission
        android:name="com.entropyjournal.permission.READ_JOURNAL"
        android:protectionLevel="normal"
        android:label="Tagebuch lesen" />

    <application
        android:name=".EntropyJournalApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.EntropyJournal"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:theme="@style/Theme.EntropyJournal"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".util.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".util.WeeklyReviewReceiver"
            android:exported="false" />

        <receiver android:name=".util.MonthlyReviewReceiver" android:exported="false" />
        <receiver android:name=".util.YearlyReviewReceiver" android:exported="false" />

        <receiver
            android:name=".util.BootReminderReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
            </intent-filter>
        </receiver>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Frank-Wunsch 2026-05-24: Nur-lesende Durchreiche der Tagebucheintraege
             fuer die App Entropie Reductor. -->
        <provider
            android:name=".data.provider.JournalExportProvider"
            android:authorities="${applicationId}.journalexport"
            android:exported="true"
            android:readPermission="com.entropyjournal.permission.READ_JOURNAL" />
    </application>

</manifest>
