diff --git a/packages/core/src/flag/flag.ts b/packages/core/src/flag/flag.ts
index a0eb78a..4710ae2 100644
--- a/packages/core/src/flag/flag.ts
+++ b/packages/core/src/flag/flag.ts
@@ -41,7 +41,7 @@ export const Flag = {
     Config.withDefault(false),
   ),
   OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT:
-    copy === undefined ? process.platform === "win32" : truthy("OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT"),
+    copy === undefined ? false : truthy("OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT"),
   OPENCODE_MODELS_URL: process.env["OPENCODE_MODELS_URL"],
   OPENCODE_MODELS_PATH: process.env["OPENCODE_MODELS_PATH"],
   OPENCODE_DB: process.env["OPENCODE_DB"],
diff --git a/packages/plugin/src/tui.ts b/packages/plugin/src/tui.ts
index 6781216..43a05a3 100644
--- a/packages/plugin/src/tui.ts
+++ b/packages/plugin/src/tui.ts
@@ -201,6 +201,8 @@ export type TuiPromptInfo = {
 export type TuiPromptRef = {
   focused: boolean
   current: TuiPromptInfo
+  contains?(x: number, y: number): boolean
+  paste?(): void
   set(prompt: TuiPromptInfo): void
   reset(): void
   blur(): void
@@ -372,6 +374,15 @@ export type TuiKV = {
   readonly ready: boolean
 }
 
+export type TuiModel = {
+  current?: () => { providerID: string; modelID: string } | undefined
+  variant: {
+    current: () => string | undefined
+    list: () => ReadonlyArray<string>
+    set: (value: string | undefined) => boolean
+  }
+}
+
 export type TuiState = {
   readonly ready: boolean
   readonly config: SdkConfig
@@ -591,6 +602,7 @@ export type TuiPluginApi = {
   keys: TuiKeys
   keymap: TuiKeymap
   mode: TuiModeApi
+  model?: TuiModel
   route: {
     register: (routes: TuiRouteDefinition[]) => () => void
     navigate: (name: string, params?: Record<string, unknown>) => void
diff --git a/packages/tui/src/app.tsx b/packages/tui/src/app.tsx
index 57f372e..758dbe0 100644
--- a/packages/tui/src/app.tsx
+++ b/packages/tui/src/app.tsx
@@ -9,7 +9,7 @@ import { ClipboardProvider, useClipboard } from "./context/clipboard"
 import { ExitProvider, useExit } from "./context/exit"
 import { EpilogueProvider } from "./context/epilogue"
 import * as Selection from "./util/selection"
-import { createCliRenderer, MouseButton } from "@opentui/core"
+import { CliRenderEvents, createCliRenderer, MouseButton } from "@opentui/core"
 import { RouteProvider, useRoute } from "./context/route"
 import {
   Switch,
@@ -384,6 +384,11 @@ function App(props: { onSnapshot?: () => Promise<string[]>; pluginHost: TuiPlugi
   const pluginRuntime = usePluginRuntime()
   const attention = createTuiAttention({ renderer, config: tuiConfig, kv })
   const clipboard = useClipboard()
+  const copyFinishedSelection = () => {
+    if (Flag.OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT) return
+    Selection.copy(renderer, toast, clipboard)
+  }
+  renderer.on(CliRenderEvents.SELECTION, copyFinishedSelection)
 
   const api = createTuiApi(
     createTuiApiAdapters({
@@ -391,6 +396,7 @@ function App(props: { onSnapshot?: () => Promise<string[]>; pluginHost: TuiPlugi
       tuiConfig,
       dialog,
       keymap,
+      local,
       kv,
       route,
       routes: pluginRuntime.routes,
@@ -430,6 +435,7 @@ function App(props: { onSnapshot?: () => Promise<string[]>; pluginHost: TuiPlugi
   )
   onCleanup(() => {
     offSelectionKeys()
+    renderer.off(CliRenderEvents.SELECTION, copyFinishedSelection)
     attention.dispose()
   })
 
@@ -1091,18 +1097,17 @@ function App(props: { onSnapshot?: () => Promise<string[]>; pluginHost: TuiPlugi
       flexDirection="column"
       backgroundColor={theme.background}
       onMouseDown={(evt) => {
-        if (!Flag.OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT) return
         if (evt.button !== MouseButton.RIGHT) return
 
-        if (!Selection.copy(renderer, toast, clipboard)) return
+        if (Flag.OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT) {
+          if (!Selection.copy(renderer, toast, clipboard)) return
+        } else {
+          if (!promptRef.current?.contains?.(evt.x, evt.y)) return
+          promptRef.current.paste?.()
+        }
         evt.preventDefault()
         evt.stopPropagation()
       }}
-      onMouseUp={
-        !Flag.OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT
-          ? () => Selection.copy(renderer, toast, clipboard)
-          : undefined
-      }
     >
       <Show when={Flag.OPENCODE_SHOW_TTFD}>
         <TimeToFirstDraw />
diff --git a/packages/tui/src/plugin/adapters.tsx b/packages/tui/src/plugin/adapters.tsx
index fef0ec8..45cbbe3 100644
--- a/packages/tui/src/plugin/adapters.tsx
+++ b/packages/tui/src/plugin/adapters.tsx
@@ -5,6 +5,7 @@ import type { useRoute } from "../context/route"
 import type { useSDK } from "../context/sdk"
 import type { useSync } from "../context/sync"
 import type { useTheme } from "../context/theme"
+import type { useLocal } from "../context/local"
 import { Dialog as DialogUI, type useDialog } from "../ui/dialog"
 import type { useOpencodeKeymap } from "../keymap"
 import type { useKV } from "../context/kv"
@@ -25,6 +26,7 @@ type Input = {
   tuiConfig: TuiConfig.Resolved
   dialog: ReturnType<typeof useDialog>
   keymap: ReturnType<typeof useOpencodeKeymap>
+  local: ReturnType<typeof useLocal>
   kv: ReturnType<typeof useKV>
   route: ReturnType<typeof useRoute>
   routes: PluginRoutes
@@ -193,6 +195,24 @@ export function createTuiApiAdapters(input: Input): Omit<TuiPluginApi, "lifecycl
         return Keymap.getOpencodeModeStack(input.keymap).push(mode)
       },
     },
+    model: {
+      current() {
+        return input.local.model.current()
+      },
+      variant: {
+        current() {
+          return input.local.model.variant.current()
+        },
+        list() {
+          return input.local.model.variant.list()
+        },
+        set(value) {
+          if (value !== undefined && !input.local.model.variant.list().includes(value)) return false
+          input.local.model.variant.set(value)
+          return input.local.model.variant.current() === value
+        },
+      },
+    },
     route: {
       register(list) {
         return input.routes.register(list)
diff --git a/packages/tui/src/clipboard.ts b/packages/tui/src/clipboard.ts
index 08f86f9..4da53aa 100644
--- a/packages/tui/src/clipboard.ts
+++ b/packages/tui/src/clipboard.ts
@@ -57,6 +57,14 @@ export async function read() {
       Buffer.alloc(0),
     )
     if (image.length) return { data: image.toString().trim(), mime: "image/png" }
+
+    const text = await command("powershell.exe", [
+      "-NonInteractive",
+      "-NoProfile",
+      "-Command",
+      "[Console]::OutputEncoding = [System.Text.UTF8Encoding]::new($false); Get-Clipboard -Raw",
+    ]).catch(() => Buffer.alloc(0))
+    if (text.length) return { data: text.toString("utf8"), mime: "text/plain" }
   }
 
   if (platform() === "linux") {
diff --git a/packages/tui/src/component/prompt/index.tsx b/packages/tui/src/component/prompt/index.tsx
index 115c102..60f5f21 100644
--- a/packages/tui/src/component/prompt/index.tsx
+++ b/packages/tui/src/component/prompt/index.tsx
@@ -88,6 +88,8 @@ function pastedFilepath(value: string, platform: string) {
 export type PromptRef = {
   focused: boolean
   current: PromptInfo
+  contains?(x: number, y: number): boolean
+  paste?(): void
   set(prompt: PromptInfo): void
   reset(): void
   blur(): void
@@ -372,18 +374,7 @@ export function Prompt(props: PromptProps) {
         run: async (ctx: CommandContext<Renderable, KeyEvent>) => {
           ctx.event.preventDefault()
           ctx.event.stopPropagation()
-          const content = await clipboard.read?.()
-          if (content?.mime.startsWith("image/")) {
-            await pasteAttachment({
-              filename: "clipboard",
-              mime: content.mime,
-              content: content.data,
-            })
-            return
-          }
-          if (content?.mime === "text/plain") {
-            await pasteInputText(content.data)
-          }
+          await pasteClipboard()
         },
       },
       {
@@ -583,6 +574,12 @@ export function Prompt(props: PromptProps) {
     get current() {
       return store.prompt
     },
+    contains(x, y) {
+      return x >= input.screenX && x < input.screenX + input.width && y >= input.screenY && y < input.screenY + input.height
+    },
+    paste() {
+      void pasteClipboard()
+    },
     focus() {
       input.focus()
     },
@@ -1218,6 +1215,19 @@ export function Prompt(props: PromptProps) {
     }, 0)
   }
 
+  async function pasteClipboard() {
+    const content = await clipboard.read?.()
+    if (content?.mime.startsWith("image/")) {
+      await pasteAttachment({
+        filename: "clipboard",
+        mime: content.mime,
+        content: content.data,
+      })
+      return
+    }
+    if (content?.mime === "text/plain") await pasteInputText(content.data)
+  }
+
   async function pasteAttachment(file: { filename?: string; filepath?: string; content: string; mime: string }) {
     const currentOffset = input.cursorOffset
     const extmarkStart = currentOffset
@@ -1431,7 +1441,7 @@ export function Prompt(props: PromptProps) {
                   input.cursorColor = theme.text
                 }, 0)
               }}
-              onMouseDown={(r: MouseEvent) => r.target?.focus()}
+              onMouseDown={(event: MouseEvent) => event.target?.focus()}
               focusedBackgroundColor={theme.backgroundElement}
               cursorColor={props.disabled ? theme.backgroundElement : theme.text}
               syntaxStyle={syntax()}
diff --git a/packages/tui/src/ui/dialog.tsx b/packages/tui/src/ui/dialog.tsx
index 5028163..8dd33ab 100644
--- a/packages/tui/src/ui/dialog.tsx
+++ b/packages/tui/src/ui/dialog.tsx
@@ -210,7 +210,6 @@ export function DialogProvider(props: ParentProps) {
           evt.preventDefault()
           evt.stopPropagation()
         }}
-        onMouseUp={!Flag.OPENCODE_EXPERIMENTAL_DISABLE_COPY_ON_SELECT ? copySelection : undefined}
       >
         <Show when={value.stack.length}>
           <Dialog onClose={() => value.clear()} size={value.size}>
diff --git a/packages/opencode/src/cli/cmd/tui.ts b/packages/opencode/src/cli/cmd/tui.ts
index 6a9af10..dc42abf 100644
--- a/packages/opencode/src/cli/cmd/tui.ts
+++ b/packages/opencode/src/cli/cmd/tui.ts
@@ -83,6 +83,10 @@ export const TuiThreadCommand = cmd({
         alias: ["m"],
         describe: "model to use in the format of provider/model",
       })
+      .option("variant", {
+        type: "string",
+        describe: "initial model variant for this TUI process",
+      })
       .option("continue", {
         alias: ["c"],
         describe: "continue the last session",
@@ -285,6 +289,7 @@ export const TuiThreadCommand = cmd({
               sessionID: args.session,
               agent: args.agent,
               model: args.model,
+              variant: args.variant,
               prompt,
               fork: args.fork,
               auto: args.auto || args.yolo || args["dangerously-skip-permissions"],
diff --git a/packages/opencode/test/cli/tui/thread.test.ts b/packages/opencode/test/cli/tui/thread.test.ts
index 73f87d9..7515121 100644
--- a/packages/opencode/test/cli/tui/thread.test.ts
+++ b/packages/opencode/test/cli/tui/thread.test.ts
@@ -66,6 +66,16 @@ describe("tui thread", () => {
     expect(args.mdns).toBe(false)
   })
 
+  test("parses an initial TUI model variant", async () => {
+    const args = await yargs([])
+      .command({ ...TuiThreadCommand, handler: () => {} })
+      .exitProcess(false)
+      .parse(["--model", "openai/gpt-5.6-sol", "--variant", "high"])
+
+    expect(args.model).toBe("openai/gpt-5.6-sol")
+    expect(args.variant).toBe("high")
+  })
+
   cliIt.live("rejects mini-only options without --mini", ({ opencode }) =>
     Effect.gen(function* () {
       const result = yield* opencode.spawn(["--replay-limit", "10"])
diff --git a/packages/tui/src/context/args.tsx b/packages/tui/src/context/args.tsx
index 9a528f2..74afc41 100644
--- a/packages/tui/src/context/args.tsx
+++ b/packages/tui/src/context/args.tsx
@@ -2,6 +2,7 @@ import { createSimpleContext } from "./helper"
 
 export interface Args {
   model?: string
+  variant?: string
   agent?: string
   prompt?: string
   continue?: boolean
diff --git a/packages/tui/src/context/local.tsx b/packages/tui/src/context/local.tsx
index a05d315..3c25af1 100644
--- a/packages/tui/src/context/local.tsx
+++ b/packages/tui/src/context/local.tsx
@@ -48,6 +48,17 @@ export function recentModels(
     .map((item) => ({ providerID: item.providerID, modelID: item.modelID }))
 }
 
+export function initialVariant(
+  model: string | undefined,
+  variant: string | undefined,
+  current: { providerID: string; modelID: string } | undefined,
+) {
+  if (!model || !variant || !current) return undefined
+  const initial = parseModel(model)
+  if (initial.providerID !== current.providerID || initial.modelID !== current.modelID) return undefined
+  return variant
+}
+
 export const { use: useLocal, provider: LocalProvider } = createSimpleContext({
   name: "Local",
   init: () => {
@@ -135,6 +146,7 @@ export const { use: useLocal, provider: LocalProvider } = createSimpleContext({
     const agent = createAgent()
 
     function createModel() {
+      let startupVariant = args.variant
       const [modelStore, setModelStore] = createStore<{
         ready: boolean
         model: Record<
@@ -364,6 +376,8 @@ export const { use: useLocal, provider: LocalProvider } = createSimpleContext({
             const m = currentModel()
             if (!m) return undefined
             const key = `${m.providerID}/${m.modelID}`
+            const initial = initialVariant(args.model, startupVariant, m)
+            if (initial) return initial
             return modelStore.variant[key]
           },
           current() {
@@ -383,6 +397,7 @@ export const { use: useLocal, provider: LocalProvider } = createSimpleContext({
           set(value: string | undefined) {
             const m = currentModel()
             if (!m) return
+            startupVariant = undefined
             const key = `${m.providerID}/${m.modelID}`
             setModelStore("variant", key, value ?? "default")
             save()
diff --git a/packages/tui/test/context/local.test.ts b/packages/tui/test/context/local.test.ts
index e2f1e45..a90a931 100644
--- a/packages/tui/test/context/local.test.ts
+++ b/packages/tui/test/context/local.test.ts
@@ -1,5 +1,5 @@
 import { expect, test } from "bun:test"
-import { parseModel, recentModels } from "../../src/context/local"
+import { initialVariant, parseModel, recentModels } from "../../src/context/local"
 
 test("parses model IDs containing slashes", () => {
   expect(parseModel("provider/family/model")).toEqual({
@@ -20,3 +20,18 @@ test("moves a model to the front, deduplicates, and limits recents", () => {
     ...recent.slice(6, 10),
   ])
 })
+
+test("applies an initial variant only to its startup model", () => {
+  expect(
+    initialVariant("openai/gpt-5.6-sol", "high", {
+      providerID: "openai",
+      modelID: "gpt-5.6-sol",
+    }),
+  ).toBe("high")
+  expect(
+    initialVariant("openai/gpt-5.6-sol", "high", {
+      providerID: "openai",
+      modelID: "gpt-5.5",
+    }),
+  ).toBeUndefined()
+})
diff --git a/packages/opencode/src/config/plugin.ts b/packages/opencode/src/config/plugin.ts
index 60bba4d..7108fd5 100644
--- a/packages/opencode/src/config/plugin.ts
+++ b/packages/opencode/src/config/plugin.ts
@@ -3,8 +3,13 @@ import { ConfigPluginV1 } from "@opencode-ai/core/v1/config/plugin"
 import { pathToFileURL } from "url"
 import { isPathPluginSpec, parsePluginSpecifier, resolvePathPluginTarget } from "@/plugin/shared"
 import path from "path"
+import { Global } from "@opencode-ai/core/global"
+import { Filesystem } from "@/util/filesystem"
+import { parse as parseJsonc } from "jsonc-parser"
+import { isRecord } from "@/util/record"
 
 export type Scope = "global" | "local"
+export const SERVER_PLUGIN_PREFIX = "server-plugin:"
 
 // Origin keeps the original config provenance attached to a spec.
 // After multiple config files are merged, callers still need to know which file declared the plugin
@@ -37,6 +42,33 @@ export function pluginOptions(plugin: ConfigPluginV1.Spec): ConfigPluginV1.Optio
   return Array.isArray(plugin) ? plugin[1] : undefined
 }
 
+export function pluginKey(plugin: ConfigPluginV1.Spec): string {
+  const spec = pluginSpecifier(plugin)
+  return spec.startsWith("file://") ? spec : parsePluginSpecifier(spec).pkg
+}
+
+export function pluginEnabled(plugin: ConfigPluginV1.Spec, enabled?: Record<string, boolean>): boolean {
+  return enabled?.[serverPluginKey(plugin)] !== false
+}
+
+export function serverPluginKey(plugin: ConfigPluginV1.Spec): string {
+  return `${SERVER_PLUGIN_PREFIX}${pluginKey(plugin)}`
+}
+
+export async function loadPluginEnabled(dir = Global.Path.config): Promise<Record<string, boolean>> {
+  const result: Record<string, boolean> = {}
+  for (const file of [path.join(dir, "tui.json"), path.join(dir, "tui.jsonc")]) {
+    const text = await Filesystem.readText(file).catch(() => undefined)
+    if (!text) continue
+    const data = parseJsonc(text, undefined, { allowTrailingComma: true })
+    if (!isRecord(data) || !isRecord(data.plugin_enabled)) continue
+    for (const [id, enabled] of Object.entries(data.plugin_enabled)) {
+      if (typeof enabled === "boolean") result[id] = enabled
+    }
+  }
+  return result
+}
+
 // Path-like specs are resolved relative to the config file that declared them so merges later on do not
 // accidentally reinterpret `./plugin.ts` relative to some other directory.
 export async function resolvePluginSpec(
@@ -66,8 +98,7 @@ export function deduplicatePluginOrigins(plugins: Origin[]): Origin[] {
   const list: Origin[] = []
 
   for (const plugin of plugins.toReversed()) {
-    const spec = pluginSpecifier(plugin.spec)
-    const name = spec.startsWith("file://") ? spec : parsePluginSpecifier(spec).pkg
+    const name = pluginKey(plugin.spec)
     if (seen.has(name)) continue
     seen.add(name)
     list.push(plugin)
diff --git a/packages/opencode/src/plugin/index.ts b/packages/opencode/src/plugin/index.ts
index 1558c70..ba90f7c 100644
--- a/packages/opencode/src/plugin/index.ts
+++ b/packages/opencode/src/plugin/index.ts
@@ -31,6 +31,7 @@ import type { WorkspaceAdapter } from "@/control-plane/types"
 import { RuntimeFlags } from "@/effect/runtime-flags"
 import { EventV2Bridge } from "@/event-v2-bridge"
 import { InstallationChannel } from "@opencode-ai/core/installation/version"
+import { ConfigPlugin } from "@/config/plugin"
 
 type State = {
   hooks: Hooks[]
@@ -174,7 +175,10 @@ const layer = Layer.effect(
           if (init._tag === "Some") hooks.push(init.value)
         }
 
-        const plugins = flags.pure ? [] : (cfg.plugin_origins ?? [])
+        const enabled = yield* Effect.promise(() => ConfigPlugin.loadPluginEnabled())
+        const plugins = flags.pure
+          ? []
+          : (cfg.plugin_origins ?? []).filter((plugin) => ConfigPlugin.pluginEnabled(plugin.spec, enabled))
         if (flags.pure && cfg.plugin_origins?.length) {
         }
         if (plugins.length) yield* config.waitForDependencies()
diff --git a/packages/opencode/src/plugin/install.ts b/packages/opencode/src/plugin/install.ts
index f07b78b..0268163 100644
--- a/packages/opencode/src/plugin/install.ts
+++ b/packages/opencode/src/plugin/install.ts
@@ -75,6 +75,16 @@ type PatchOne = Ok<{ item: PatchItem }> | PatchErr
 
 export type PatchResult = Ok<{ dir: string; items: PatchItem[] }> | (PatchErr & { dir: string })
 
+export type PatchPluginEnabledInput = Omit<PatchInput, "spec" | "targets" | "force"> & {
+  id: string
+  enabled: boolean
+}
+
+export type PatchPluginEnabledResult =
+  | Ok<{ file: string }>
+  | Err<"invalid_json", { file: string; line: number; col: number; parse: string }>
+  | Err<"patch_failed", { file: string; error: unknown }>
+
 const defaultInstallDeps: InstallDeps = {
   resolve: (spec) => resolvePluginTarget(spec),
 }
@@ -437,3 +447,47 @@ export async function patchPluginConfig(input: PatchInput, dep: PatchDeps = defa
     items,
   }
 }
+
+export async function patchPluginEnabled(
+  input: PatchPluginEnabledInput,
+  dep: PatchDeps = defaultPatchDeps,
+): Promise<PatchPluginEnabledResult> {
+  const dir = patchDir({ ...input, spec: "", targets: [] })
+  await using _ = await Flock.acquire(`plug-config:${Filesystem.resolve(path.join(dir, "tui"))}`)
+
+  const files = dep.files(dir, "tui")
+  let file = files[0]
+  let found = false
+  for (const candidate of files) {
+    if (!(await dep.exists(candidate))) continue
+    if (!found || candidate.endsWith(".jsonc")) file = candidate
+    found = true
+  }
+
+  const src = await dep.readText(file).catch((err: NodeJS.ErrnoException) => {
+    if (err.code === "ENOENT") return "{}"
+    return err
+  })
+  if (src instanceof Error) return { ok: false, code: "patch_failed", file, error: src }
+
+  const text = src.trim() ? src : "{}"
+  const errs: JsoncParseError[] = []
+  parseJsonc(text, errs, { allowTrailingComma: true })
+  if (errs.length) {
+    const err = errs[0]
+    const lines = text.substring(0, err.offset).split("\n")
+    return {
+      ok: false,
+      code: "invalid_json",
+      file,
+      line: lines.length,
+      col: lines[lines.length - 1].length + 1,
+      parse: printParseErrorCode(err.error),
+    }
+  }
+
+  const updated = patch(text, ["plugin_enabled", input.id], input.enabled)
+  const write = await dep.write(file, updated).catch((error: unknown) => error)
+  if (write instanceof Error) return { ok: false, code: "patch_failed", file, error: write }
+  return { ok: true, file }
+}
diff --git a/packages/opencode/src/plugin/tui/runtime.ts b/packages/opencode/src/plugin/tui/runtime.ts
index 2a88ad3..47e39c6 100644
--- a/packages/opencode/src/plugin/tui/runtime.ts
+++ b/packages/opencode/src/plugin/tui/runtime.ts
@@ -23,12 +23,18 @@ import {
   readPluginId,
   readV1Plugin,
   resolvePluginId,
+  pluginSource,
   type PluginPackage,
   type PluginSource,
 } from "@/plugin/shared"
 import { PluginLoader } from "@/plugin/loader"
 import { PluginMeta } from "@/plugin/meta"
-import { installPlugin as installModulePlugin, patchPluginConfig, readPluginManifest } from "@/plugin/install"
+import {
+  installPlugin as installModulePlugin,
+  patchPluginConfig,
+  patchPluginEnabled,
+  readPluginManifest,
+} from "@/plugin/install"
 import { hasTheme, upsertTheme } from "@opencode-ai/tui/context/theme"
 import { Global } from "@opencode-ai/core/global"
 import { Filesystem } from "@/util/filesystem"
@@ -117,6 +123,7 @@ type RuntimeState = {
   plugins_by_id: Map<string, PluginEntry>
   pending: Map<string, ConfigPlugin.Origin>
   dispose_timeout_ms: number
+  config: TuiConfig.Resolved
 }
 
 const DISPOSE_TIMEOUT_MS = 5000
@@ -489,7 +496,7 @@ function writePluginEnabledState(api: Api, id: string, enabled: boolean) {
 }
 
 function listPluginStatus(state: RuntimeState): TuiPluginStatus[] {
-  return state.plugins.map((plugin) => ({
+  const tui = state.plugins.map((plugin) => ({
     id: plugin.id,
     source: plugin.meta.source,
     spec: plugin.meta.spec,
@@ -497,6 +504,62 @@ function listPluginStatus(state: RuntimeState): TuiPluginStatus[] {
     enabled: plugin.enabled,
     active: plugin.scope !== undefined,
   }))
+  const enabled = pluginEnabledState(state, state.config)
+  const server = (state.api.state.config.plugin ?? []).map((spec) => {
+    const item = spec as ConfigPluginV1.Spec
+    const value = ConfigPlugin.pluginSpecifier(item)
+    const key = ConfigPlugin.serverPluginKey(item)
+    const active = enabled[key] !== false
+    return {
+      id: key,
+      source: pluginSource(value),
+      spec: value,
+      target: value,
+      enabled: active,
+      active,
+    }
+  })
+  return [...tui, ...server]
+}
+
+async function setServerPluginEnabled(state: RuntimeState, id: string, enabled: boolean) {
+  const previous = pluginEnabledState(state, state.config)[id] !== false
+  const result = await patchPluginEnabled({
+    id,
+    enabled,
+    global: true,
+    directory: state.directory,
+    worktree: state.api.state.path.worktree,
+    config: state.api.state.path.config || undefined,
+  })
+  if (!result.ok) {
+    fail("failed to persist server plugin state", { id, error: result })
+    return false
+  }
+
+  const disposed = await state.api.client.global.dispose({ throwOnError: true }).then(
+    () => true,
+    (error) => {
+      fail("failed to reload server plugins", { id, error })
+      return false
+    },
+  )
+  if (!disposed) {
+    const rollback = await patchPluginEnabled({
+      id,
+      enabled: previous,
+      global: true,
+      directory: state.directory,
+      worktree: state.api.state.path.worktree,
+      config: state.api.state.path.config || undefined,
+    })
+    if (!rollback.ok) fail("failed to roll back server plugin state", { id, error: rollback })
+    return false
+  }
+
+  writePluginEnabledState(state.api, id, enabled)
+  state.view.update({ status: listPluginStatus(state) })
+  return true
 }
 
 async function deactivatePluginEntry(state: RuntimeState, plugin: PluginEntry, persist: boolean) {
@@ -557,6 +620,7 @@ async function activatePluginEntry(state: RuntimeState, plugin: PluginEntry, per
 
 async function activatePluginById(state: RuntimeState | undefined, id: string, persist: boolean) {
   if (!state) return false
+  if (id.startsWith(ConfigPlugin.SERVER_PLUGIN_PREFIX)) return setServerPluginEnabled(state, id, true)
   const plugin = state.plugins_by_id.get(id)
   if (!plugin) return false
   return activatePluginEntry(state, plugin, persist)
@@ -564,6 +628,7 @@ async function activatePluginById(state: RuntimeState | undefined, id: string, p
 
 async function deactivatePluginById(state: RuntimeState | undefined, id: string, persist: boolean) {
   if (!state) return false
+  if (id.startsWith(ConfigPlugin.SERVER_PLUGIN_PREFIX)) return setServerPluginEnabled(state, id, false)
   const plugin = state.plugins_by_id.get(id)
   if (!plugin) return false
   return deactivatePluginEntry(state, plugin, persist)
@@ -617,6 +682,7 @@ function pluginApi(runtime: RuntimeState, plugin: PluginEntry, scope: PluginScop
     keys: api.keys,
     keymap,
     mode: createScopedMode(api.mode, scope),
+    model: api.model,
     route,
     ui: api.ui,
     tuiConfig: api.tuiConfig,
@@ -651,6 +717,10 @@ function pluginApi(runtime: RuntimeState, plugin: PluginEntry, scope: PluginScop
 }
 
 function addPluginEntry(state: RuntimeState, plugin: PluginEntry) {
+  if (plugin.id.startsWith(ConfigPlugin.SERVER_PLUGIN_PREFIX)) {
+    fail("reserved tui plugin id", { id: plugin.id, path: plugin.load.spec })
+    return false
+  }
   if (state.plugins_by_id.has(plugin.id)) {
     fail("duplicate tui plugin id", {
       id: plugin.id,
@@ -1068,6 +1137,7 @@ async function load(input: {
     plugins_by_id: new Map(),
     pending: new Map(),
     dispose_timeout_ms: input.disposeTimeoutMs ?? DISPOSE_TIMEOUT_MS,
+    config,
   }
   runtime = next
   next.view.update({
diff --git a/packages/opencode/test/fixture/tui-plugin.ts b/packages/opencode/test/fixture/tui-plugin.ts
index 5d93139..b89f441 100644
--- a/packages/opencode/test/fixture/tui-plugin.ts
+++ b/packages/opencode/test/fixture/tui-plugin.ts
@@ -90,6 +90,7 @@ type Opts = {
   attention?: AttentionOpts
   event?: HostPluginApi["event"]
   mode?: HostPluginApi["mode"]
+  model?: NonNullable<HostPluginApi["model"]>
   count?: Count
   keymap?: HostPluginApi["keymap"]
   tuiConfig?: Partial<HostPluginApi["tuiConfig"]>
@@ -242,6 +243,7 @@ export function createTuiPluginApi(opts: Opts = {}): HostPluginApi {
       current: () => "base",
       push: () => () => {},
     },
+    model: opts.model,
     route: {
       register: () => {
         if (count) count.route_add += 1
diff --git a/packages/opencode/test/cli/tui/plugin-toggle.test.ts b/packages/opencode/test/cli/tui/plugin-toggle.test.ts
index 4702655..fb5da21 100644
--- a/packages/opencode/test/cli/tui/plugin-toggle.test.ts
+++ b/packages/opencode/test/cli/tui/plugin-toggle.test.ts
@@ -6,6 +6,7 @@ import { tmpdir } from "../../fixture/fixture"
 import { createTuiPluginApi } from "../../fixture/tui-plugin"
 import { createTuiResolvedConfig } from "../../fixture/tui-runtime"
 import { TuiConfig } from "../../../src/config/tui"
+import type { HostPluginApi } from "@opencode-ai/tui/plugin/slots"
 
 const { TuiPluginRuntime } = await import("../../../src/plugin/tui/runtime")
 
@@ -22,7 +23,7 @@ test("toggles plugin runtime state by exported id", async () => {
   id: "demo.toggle",
   tui: async (api, options) => {
     const text = await Bun.file(options.marker).text().catch(() => "")
-    await Bun.write(options.marker, text + "start\\n")
+    await Bun.write(options.marker, text + "start:" + api.model?.variant.current() + "\\n")
     api.lifecycle.onDispose(async () => {
       const next = await Bun.file(options.marker).text().catch(() => "")
       await Bun.write(options.marker, next + "stop\\n")
@@ -55,7 +56,15 @@ test("toggles plugin runtime state by exported id", async () => {
   })
   const wait = spyOn(TuiConfig, "waitForDependencies").mockResolvedValue()
   const cwd = spyOn(process, "cwd").mockImplementation(() => tmp.path)
-  const api = createTuiPluginApi()
+  const api = createTuiPluginApi({
+    model: {
+      variant: {
+        current: () => "high",
+        list: () => ["low", "medium", "high", "xhigh"],
+        set: () => true,
+      },
+    },
+  })
 
   try {
     await TuiPluginRuntime.init({ api, config })
@@ -71,13 +80,13 @@ test("toggles plugin runtime state by exported id", async () => {
     })
 
     await expect(TuiPluginRuntime.activatePlugin("demo.toggle")).resolves.toBe(true)
-    await expect(fs.readFile(tmp.extra.marker, "utf8")).resolves.toBe("start\n")
+    await expect(fs.readFile(tmp.extra.marker, "utf8")).resolves.toBe("start:high\n")
     expect(api.kv.get("plugin_enabled", {})).toEqual({
       "demo.toggle": true,
     })
 
     await expect(TuiPluginRuntime.deactivatePlugin("demo.toggle")).resolves.toBe(true)
-    await expect(fs.readFile(tmp.extra.marker, "utf8")).resolves.toBe("start\nstop\n")
+    await expect(fs.readFile(tmp.extra.marker, "utf8")).resolves.toBe("start:high\nstop\n")
     expect(api.kv.get("plugin_enabled", {})).toEqual({
       "demo.toggle": false,
     })
@@ -262,3 +271,107 @@ test("loads disabled-by-default internal plugin inactive and activates on demand
     wait.mockRestore()
   }
 })
+
+test("lists and toggles server plugins from the same plugin manager", async () => {
+  await using tmp = await tmpdir()
+  const wait = spyOn(TuiConfig, "waitForDependencies").mockResolvedValue()
+  const cwd = spyOn(process, "cwd").mockImplementation(() => tmp.path)
+  let disposals = 0
+  const client = {
+    global: {
+      dispose: async () => {
+        disposals += 1
+        return { data: true }
+      },
+    },
+  } as unknown as HostPluginApi["client"]
+  const api = createTuiPluginApi({
+    client,
+    state: {
+      config: { plugin: ["@mohak34/opencode-notifier@0.2.8"] },
+      path: {
+        state: path.join(tmp.path, "state"),
+        config: tmp.path,
+        worktree: tmp.path,
+        directory: tmp.path,
+      },
+    },
+  })
+  const config = createTuiResolvedConfig()
+  const id = "server-plugin:@mohak34/opencode-notifier"
+
+  try {
+    await TuiPluginRuntime.init({ api, config })
+    expect(TuiPluginRuntime.list().find((item) => item.id === id)).toMatchObject({
+      spec: "@mohak34/opencode-notifier@0.2.8",
+      enabled: true,
+      active: true,
+    })
+
+    await expect(TuiPluginRuntime.deactivatePlugin(id)).resolves.toBe(true)
+    expect(TuiPluginRuntime.list().find((item) => item.id === id)).toMatchObject({
+      enabled: false,
+      active: false,
+    })
+    const disabled = JSON.parse(await fs.readFile(path.join(tmp.path, "tui.json"), "utf8")) as {
+      plugin_enabled?: Record<string, boolean>
+    }
+    expect(disabled.plugin_enabled).toEqual({
+      "server-plugin:@mohak34/opencode-notifier": false,
+    })
+
+    await expect(TuiPluginRuntime.activatePlugin(id)).resolves.toBe(true)
+    const enabled = JSON.parse(await fs.readFile(path.join(tmp.path, "tui.json"), "utf8")) as {
+      plugin_enabled?: Record<string, boolean>
+    }
+    expect(enabled.plugin_enabled).toEqual({
+      "server-plugin:@mohak34/opencode-notifier": true,
+    })
+    expect(disposals).toBe(2)
+  } finally {
+    await TuiPluginRuntime.dispose()
+    cwd.mockRestore()
+    wait.mockRestore()
+  }
+})
+
+test("rolls back server plugin state when runtime reload fails", async () => {
+  await using tmp = await tmpdir()
+  const wait = spyOn(TuiConfig, "waitForDependencies").mockResolvedValue()
+  const cwd = spyOn(process, "cwd").mockImplementation(() => tmp.path)
+  const client = {
+    global: {
+      dispose: async () => {
+        throw new Error("reload failed")
+      },
+    },
+  } as unknown as HostPluginApi["client"]
+  const api = createTuiPluginApi({
+    client,
+    state: {
+      config: { plugin: ["demo@1.0.0"] },
+      path: {
+        state: path.join(tmp.path, "state"),
+        config: tmp.path,
+        worktree: tmp.path,
+        directory: tmp.path,
+      },
+    },
+  })
+  const id = "server-plugin:demo"
+
+  try {
+    await TuiPluginRuntime.init({ api, config: createTuiResolvedConfig() })
+    await expect(TuiPluginRuntime.deactivatePlugin(id)).resolves.toBe(false)
+    expect(TuiPluginRuntime.list().find((item) => item.id === id)).toMatchObject({ enabled: true, active: true })
+    expect(api.kv.get("plugin_enabled", {})).toEqual({})
+    const stored = JSON.parse(await fs.readFile(path.join(tmp.path, "tui.json"), "utf8")) as {
+      plugin_enabled?: Record<string, boolean>
+    }
+    expect(stored.plugin_enabled).toEqual({ "server-plugin:demo": true })
+  } finally {
+    await TuiPluginRuntime.dispose()
+    cwd.mockRestore()
+    wait.mockRestore()
+  }
+})
diff --git a/packages/opencode/test/config/config.test.ts b/packages/opencode/test/config/config.test.ts
index e281831..3a896ea 100644
--- a/packages/opencode/test/config/config.test.ts
+++ b/packages/opencode/test/config/config.test.ts
@@ -1810,6 +1810,39 @@ describe("deduplicatePluginOrigins", () => {
   )
 })
 
+describe("pluginEnabled", () => {
+  test("uses package name across pinned versions", () => {
+    expect(ConfigPlugin.pluginKey("@mohak34/opencode-notifier@0.2.8")).toBe("@mohak34/opencode-notifier")
+    expect(ConfigPlugin.serverPluginKey("@mohak34/opencode-notifier@0.2.8")).toBe(
+      "server-plugin:@mohak34/opencode-notifier",
+    )
+    expect(
+      ConfigPlugin.pluginEnabled("@mohak34/opencode-notifier@0.2.8", {
+        "@mohak34/opencode-notifier": true,
+        "server-plugin:@mohak34/opencode-notifier": false,
+      }),
+    ).toBe(false)
+  })
+
+  test("uses exact file URL for local plugins", () => {
+    const spec = "file:///project/.opencode/plugins/demo.js"
+    expect(ConfigPlugin.pluginKey(spec)).toBe(spec)
+    expect(ConfigPlugin.pluginEnabled(spec, { [`server-plugin:${spec}`]: false })).toBe(false)
+    expect(ConfigPlugin.pluginEnabled(spec, {})).toBe(true)
+  })
+
+  test("loads enabled state from global tui config without importing the TUI runtime", async () => {
+    await using tmp = await tmpdir()
+    await Bun.write(
+      path.join(tmp.path, "tui.json"),
+      JSON.stringify({ plugin_enabled: { "server-plugin:@mohak34/opencode-notifier": false, ignored: "no" } }),
+    )
+    expect(await ConfigPlugin.loadPluginEnabled(tmp.path)).toEqual({
+      "server-plugin:@mohak34/opencode-notifier": false,
+    })
+  })
+})
+
 describe("OPENCODE_DISABLE_PROJECT_CONFIG", () => {
   it.instance(
     "skips project config files when flag is set",
diff --git a/packages/opencode/test/plugin/install.test.ts b/packages/opencode/test/plugin/install.test.ts
index 6dc9175..26efc7d 100644
--- a/packages/opencode/test/plugin/install.test.ts
+++ b/packages/opencode/test/plugin/install.test.ts
@@ -4,6 +4,7 @@ import path from "path"
 import { parse as parseJsonc } from "jsonc-parser"
 import { Filesystem } from "@/util/filesystem"
 import { createPlugTask, type PlugCtx, type PlugDeps } from "../../src/cli/cmd/plug"
+import { patchPluginEnabled } from "../../src/plugin/install"
 import { tmpdir } from "../fixture/fixture"
 
 function deps(global: string, target: string | Error): PlugDeps {
@@ -105,9 +106,73 @@ async function plugin(
 async function read(file: string) {
   return Filesystem.readJson<{
     plugin?: unknown[]
+    plugin_enabled?: Record<string, boolean>
   }>(file)
 }
 
+test("patchPluginEnabled persists runtime state in tui config", async () => {
+  await using tmp = await tmpdir()
+  const global = path.join(tmp.path, "global")
+  const dep = deps(global, "unused")
+
+  const disabled = await patchPluginEnabled(
+    {
+      id: "server-plugin:@mohak34/opencode-notifier",
+      enabled: false,
+      global: true,
+      config: global,
+      worktree: tmp.path,
+      directory: tmp.path,
+    },
+    dep,
+  )
+  expect(disabled.ok).toBe(true)
+  expect((await read(path.join(global, "tui.jsonc"))).plugin_enabled).toEqual({
+    "server-plugin:@mohak34/opencode-notifier": false,
+  })
+
+  const enabled = await patchPluginEnabled(
+    {
+      id: "server-plugin:@mohak34/opencode-notifier",
+      enabled: true,
+      global: true,
+      config: global,
+      worktree: tmp.path,
+      directory: tmp.path,
+    },
+    dep,
+  )
+  expect(enabled.ok).toBe(true)
+  expect((await read(path.join(global, "tui.jsonc"))).plugin_enabled).toEqual({
+    "server-plugin:@mohak34/opencode-notifier": true,
+  })
+})
+
+test("patchPluginEnabled writes the higher-precedence jsonc file when both configs exist", async () => {
+  await using tmp = await tmpdir()
+  const global = path.join(tmp.path, "global")
+  const dep = deps(global, "unused")
+  await fs.mkdir(global, { recursive: true })
+  await Bun.write(path.join(global, "tui.json"), JSON.stringify({ plugin_enabled: { "server-plugin:demo": true } }))
+  await Bun.write(path.join(global, "tui.jsonc"), '{ "plugin_enabled": { "server-plugin:demo": true } }')
+
+  const result = await patchPluginEnabled(
+    {
+      id: "server-plugin:demo",
+      enabled: false,
+      global: true,
+      config: global,
+      worktree: tmp.path,
+      directory: tmp.path,
+    },
+    dep,
+  )
+
+  expect(result).toEqual({ ok: true, file: path.join(global, "tui.jsonc") })
+  expect((await read(path.join(global, "tui.json"))).plugin_enabled).toEqual({ "server-plugin:demo": true })
+  expect((await read(path.join(global, "tui.jsonc"))).plugin_enabled).toEqual({ "server-plugin:demo": false })
+})
+
 describe("plugin.install.task", () => {
   test("writes both server and tui config entries", async () => {
     await using tmp = await tmpdir()
diff --git a/packages/tui/src/feature-plugins/system/plugins.tsx b/packages/tui/src/feature-plugins/system/plugins.tsx
index 78611e0..6716070 100644
--- a/packages/tui/src/feature-plugins/system/plugins.tsx
+++ b/packages/tui/src/feature-plugins/system/plugins.tsx
@@ -15,11 +15,21 @@ function state(api: TuiPluginApi, item: TuiPluginStatus) {
 
   return (
     <span style={{ fg: item.active ? api.theme.current.success : api.theme.current.error }}>
-      {item.active ? "active" : "inactive"}
+      {item.id.startsWith("server-plugin:")
+        ? item.active
+          ? "enabled"
+          : "disabled"
+        : item.active
+          ? "active"
+          : "inactive"}
     </span>
   )
 }
 
+function title(item: TuiPluginStatus) {
+  return item.id.startsWith("server-plugin:") ? item.id.slice("server-plugin:".length) : item.id
+}
+
 function source(spec: string) {
   if (!spec.startsWith("file://")) return
   return fileURLToPath(spec)
@@ -134,9 +144,9 @@ function Install(props: { api: TuiPluginApi }) {
 
 function row(api: TuiPluginApi, item: TuiPluginStatus, width: number): DialogSelectOption<string> {
   return {
-    title: item.id,
+    title: title(item),
     value: item.id,
-    category: item.source === "internal" ? "Internal" : "External",
+    category: item.id.startsWith("server-plugin:") ? "Runtime" : item.source === "internal" ? "Internal" : "TUI",
     description: meta(item, width),
     footer: state(api, item),
     disabled: item.id === id,
diff --git a/packages/tui/src/feature-plugins/sidebar/context.tsx b/packages/tui/src/feature-plugins/sidebar/context.tsx
index f1c99d9..7c31f62 100644
--- a/packages/tui/src/feature-plugins/sidebar/context.tsx
+++ b/packages/tui/src/feature-plugins/sidebar/context.tsx
@@ -1,61 +1,9 @@
-import type { AssistantMessage } from "@opencode-ai/sdk/v2"
-import type { TuiPlugin, TuiPluginApi } from "@opencode-ai/plugin/tui"
+import type { TuiPlugin } from "@opencode-ai/plugin/tui"
 import type { BuiltinTuiPlugin } from "../builtins"
-import { createMemo } from "solid-js"
 
 const id = "internal:sidebar-context"
 
-const money = new Intl.NumberFormat("en-US", {
-  style: "currency",
-  currency: "USD",
-})
-
-function View(props: { api: TuiPluginApi; session_id: string }) {
-  const theme = () => props.api.theme.current
-  const msg = createMemo(() => props.api.state.session.messages(props.session_id))
-  const session = createMemo(() => props.api.state.session.get(props.session_id))
-  const cost = createMemo(() => session()?.cost ?? 0)
-
-  const state = createMemo(() => {
-    const last = msg().findLast((item): item is AssistantMessage => item.role === "assistant" && item.tokens.output > 0)
-    if (!last) {
-      return {
-        tokens: 0,
-        percent: null,
-      }
-    }
-
-    const tokens =
-      last.tokens.input + last.tokens.output + last.tokens.reasoning + last.tokens.cache.read + last.tokens.cache.write
-    const model = props.api.state.provider.find((item) => item.id === last.providerID)?.models[last.modelID]
-    return {
-      tokens,
-      percent: model?.limit.context ? Math.round((tokens / model.limit.context) * 100) : null,
-    }
-  })
-
-  return (
-    <box>
-      <text fg={theme().text}>
-        <b>Context</b>
-      </text>
-      <text fg={theme().textMuted}>{state().tokens.toLocaleString()} tokens</text>
-      <text fg={theme().textMuted}>{state().percent ?? 0}% used</text>
-      <text fg={theme().textMuted}>{money.format(cost())} spent</text>
-    </box>
-  )
-}
-
-const tui: TuiPlugin = async (api) => {
-  api.slots.register({
-    order: 100,
-    slots: {
-      sidebar_content(_ctx, props) {
-        return <View api={api} session_id={props.session_id} />
-      },
-    },
-  })
-}
+const tui: TuiPlugin = async () => {}
 
 const plugin: BuiltinTuiPlugin = {
   id,
diff --git a/packages/tui/src/feature-plugins/sidebar/lsp.tsx b/packages/tui/src/feature-plugins/sidebar/lsp.tsx
index c260cfb..e194f3c 100644
--- a/packages/tui/src/feature-plugins/sidebar/lsp.tsx
+++ b/packages/tui/src/feature-plugins/sidebar/lsp.tsx
@@ -1,3 +1,4 @@
 import type { TuiPlugin, TuiPluginApi } from "@opencode-ai/plugin/tui"
+import { TextAttributes } from "@opentui/core"
 import type { BuiltinTuiPlugin } from "../builtins"
 import { createMemo, For, Show, createSignal } from "solid-js"
@@ -16,7 +17,5 @@ function View(props: { api: TuiPluginApi }) {
         <Show when={list().length > 2}>
           <text fg={theme().text}>{open() ? "▼" : "▶"}</text>
         </Show>
-        <text fg={theme().text}>
-          <b>LSP</b>
-        </text>
+        <text fg={theme().accent} attributes={TextAttributes.BOLD | TextAttributes.UNDERLINE}>LSP</text>
       </box>
diff --git a/packages/tui/src/feature-plugins/sidebar/mcp.tsx b/packages/tui/src/feature-plugins/sidebar/mcp.tsx
index 99d8567..83c2db8 100644
--- a/packages/tui/src/feature-plugins/sidebar/mcp.tsx
+++ b/packages/tui/src/feature-plugins/sidebar/mcp.tsx
@@ -33,7 +33,7 @@ function View(props: { api: TuiPluginApi }) {
           <Show when={list().length > 2}>
             <text fg={theme().text}>{open() ? "▼" : "▶"}</text>
           </Show>
-          <text fg={theme().text}>
+          <text fg={theme().accent}>
-            <b>MCP</b>
+            <span style={{ bold: true, underline: true }}>MCP</span>
             <Show when={!open()}>
               <span style={{ fg: theme().textMuted }}>
